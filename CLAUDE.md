# CLAUDE.md

Essential guidance for working with this codebase. For detailed architecture, see [ARCHITECTURE.md](ARCHITECTURE.md). For development setup and processes, see [CONTRIBUTING.md](CONTRIBUTING.md).

## Documentation Index

This guide has been split into focused sub-documents for progressive disclosure:

| Document                                   | Description                                                                                                                                                              |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [[CLAUDE-PATTERNS.md]]                     | Core implementation patterns (process management, settings, modals, themes, Auto Run, SSH, Encore Features)                                                              |
| [[CLAUDE-IPC.md]]                          | IPC API surface (`window.maestro.*` namespaces)                                                                                                                          |
| [[CLAUDE-PERFORMANCE.md]]                  | Performance best practices (React optimization, debouncing, batching)                                                                                                    |
| [[CLAUDE-WIZARD.md]]                       | Onboarding Wizard, Inline Wizard, and Tour System                                                                                                                        |
| [[CLAUDE-FEATURES.md]]                     | Usage Dashboard and Document Graph features                                                                                                                              |
| [[CLAUDE-AGENTS.md]]                       | Supported agents and capabilities                                                                                                                                        |
| [[CLAUDE-SESSION.md]]                      | Session interface (agent data model) and code conventions                                                                                                                |
| [[CLAUDE-PLATFORM.md]]                     | Cross-platform concerns (Windows, Linux, macOS, SSH remote)                                                                                                              |
| [[CLAUDE-CUE.md]]                          | Cue automation engine: architecture, dispatch flow, lifecycle, gotchas (read before editing `src/main/cue/`)                                                             |
| [[CLAUDE-PLUGINS.md]]                      | Plugin system architecture: tiers, sandbox, broker, capabilities, contributions, signing, trust model (read before editing `src/main/plugins/` or `src/shared/plugins/`) |
| [[CLAUDE-SETTINGS.md]]                     | Settings modal style guide: typography, color, dimming, spacing, primitives, registration checklist (read before editing `src/renderer/components/Settings/`)            |
| [PROVIDER-SUPPORT.md](PROVIDER-SUPPORT.md) | Detailed agent integration guide                                                                                                                                         |

---

## Before Writing New Code - Check Existing Utilities

**MANDATORY:** Before creating any new utility function, helper, hook, component, type, or constant, check the guide docs in `docs/agent-guides/` to see if it already exists. Duplicated code is the #1 source of maintenance burden in this codebase - there are already grep-verified instances of 20+ duplicate format helpers, 60+ ad-hoc mock factories, and 500+ manual modal-layer registrations. Don't add to the pile.

| Before creating...                                 | Check this guide first                                           |
| -------------------------------------------------- | ---------------------------------------------------------------- |
| Utility function (formatting, IDs, paths, strings) | [SHARED-UTILS.md](docs/agent-guides/SHARED-UTILS.md)             |
| IPC handler or preload bridge                      | [IPC-PATTERNS.md](docs/agent-guides/IPC-PATTERNS.md)             |
| Store action, selector, or hook                    | [STATE-PATTERNS.md](docs/agent-guides/STATE-PATTERNS.md)         |
| Agent parser, storage, or error pattern            | [AGENT-INFRA.md](docs/agent-guides/AGENT-INFRA.md)               |
| UI component, modal, or theme usage                | [UI-PATTERNS.md](docs/agent-guides/UI-PATTERNS.md)               |
| Stat card, chart, sparkline, or input control      | [WIDGET-LIBRARY.md](docs/agent-guides/WIDGET-LIBRARY.md)         |
| Test mock, factory, or setup pattern               | [TEST-PATTERNS.md](docs/agent-guides/TEST-PATTERNS.md)           |
| Renderer service or constant                       | [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md)   |
| Process spawning or listener                       | [PROCESS-SYSTEM.md](docs/agent-guides/PROCESS-SYSTEM.md)         |
| Browser (web-desktop) UI, PWA, or touch hook       | [WEB-MOBILE.md](docs/agent-guides/WEB-MOBILE.md)                 |
| CLI command or playbook feature                    | [CLI-PLAYBOOKS.md](docs/agent-guides/CLI-PLAYBOOKS.md)           |
| CLI verb mirroring a point-and-click action        | [CLI-UI-PARITY.md](docs/agent-guides/CLI-UI-PARITY.md)           |
| Group chat or Symphony feature                     | [GROUP-CHAT.md](docs/agent-guides/GROUP-CHAT.md)                 |
| Stats, analytics, or dashboard                     | [STATS-ANALYTICS.md](docs/agent-guides/STATS-ANALYTICS.md)       |
| Prompt template or SpecKit/OpenSpec                | [PROMPTS-SPECS.md](docs/agent-guides/PROMPTS-SPECS.md)           |
| Cue pipeline feature                               | [CUE-PIPELINE.md](docs/agent-guides/CUE-PIPELINE.md)             |
| App lifecycle, updater, or power mgmt              | [MAIN-LIFECYCLE.md](docs/agent-guides/MAIN-LIFECYCLE.md)         |
| Plugin, sandbox capability, or contribution        | [PLUGIN-DEVELOPMENT.md](docs/agent-guides/PLUGIN-DEVELOPMENT.md) |
| React context, renderer type, or Symphony runner   | [REMAINING-SYSTEMS.md](docs/agent-guides/REMAINING-SYSTEMS.md)   |

### Commonly-reimplemented functions (do NOT add new copies)

Grep-verified 2026-09-04 (`npm run docs:verify` re-checks every path). This is the INDEX: name, canonical symbols, home file. The full entry for each - the failure it replaced, its invariants, and the traps that made the duplicates wrong - lives in [CANONICAL-UTILITIES.md](docs/agent-guides/CANONICAL-UTILITIES.md). If a name below matches what you are about to write, import the canonical one; read its full entry before extending or working around it.

- **ID generation:** `generateId()`, `generateUUID()` in `src/renderer/utils/ids.ts`
- **Format file size:** `formatSize()` in `src/shared/formatters.ts`
- **Format numbers:** `formatNumber()`, `formatCount()` in `src/shared/formatters.ts`
- **Format tokens:** `formatTokens()`, `formatTokensCompact()` in `src/shared/formatters.ts`
- **Format elapsed time:** `formatElapsedTimeColon()` in `src/shared/formatters.ts`
- **Humanized durations:** everything in `src/shared/duration.ts` (re-exported from `formatters.ts`). `humanizeDuration(ms, opts)` is the one engine; `formatDurationHuman` / `Compact` / `Verbose` / `Parts` / `Decimal` / `Long` / `Words`, plus `formatActiveTime`, `formatElapsedTime`, `formatElapsedTicker` (thinking pill: `"0m 3s"`), and `formatElapsedTickerCompact` (the same ladder without the padded lead, for a counter inside a chip: `"3s"`, `"20m 4s"`), are presets over it. **Never hand-roll another `Math.floor(ms / 86400000)` ladder** - a dozen near-identical copies had already drifted apart on where the ladder stops, whether a zero segment is padded (`"2h 0m"` vs `"2h"`), and whether a countdown rounds up. Those are options (`units`, `maxUnits`, `style`, `keepZeroUnits`, `keepLeadingZero`, `adjacentUnits`, `round`, `fallback`), so pick a preset or call the engine. Unit sizes are in `DURATION_MS` - don't redeclare `const DAY = 86400000`. See [SHARED-UTILS.md → Durations](docs/agent-guides/SHARED-UTILS.md#durations-srcshareddurationts---both).
- **Format profiling spans (ms):** `formatDuration()` in `src/shared/performance-metrics.ts` (NOT formatters.ts - common mistake). Two-decimal `"123.45ms"` / `"1.23s"` for perf traces only; it caps at seconds, so a multi-day span comes out as a giant seconds float. For anything user-facing use `src/shared/duration.ts`.
- **Format relative time:** `formatRelativeTime()` in `src/shared/formatters.ts`
- **Format a calendar day:** `formatCalendarDay()` in `src/shared/formatters.ts` renders a `YYYY-MM-DD` string as `"Jul 10, 2026"`. It reads the parts out of the string and builds a LOCAL date, because `new Date('2026-07-10')` is parsed as UTC midnight and renders the day before everywhere west of Greenwich.
- **Format cost:** `formatCost()` in `src/shared/formatters.ts`
- **Token pricing and Codex token totals:** `resolveModelPricing()` / `calculateModelCost()` in `src/shared/modelPricing.ts`, and `CodexTokenCounts` in `src/shared/codexTokenUsage.ts`. Pricing is not Claude-only: `resolveModelPricing` matches the OpenAI families first (`gpt-5*` / any `codex` slug, split nano / mini / frontier), since a Codex slug carries no Anthropic family word and used to fall through to the Sonnet default. The two providers also disagree about what `input_tokens` MEANS - Anthropic reports cache reads beside it, OpenAI inside it - so `PricingConfig.CACHE_READ_SUBSET_OF_INPUT` rides on the tier and `calculateWithPricing` bills only the uncached remainder; without it a cache hit costs MORE than a miss. On the parsing side, Codex's `info.total_token_usage` is a running SESSION TOTAL while `turn.completed.usage` and `info.last_token_usage` are per-turn, so summing the first makes a session report tokens proportional to the SQUARE of its turn count (1.3B real input tokens were reported as 75B). Every Codex transcript scan goes through `CodexTokenCounts`; do NOT hand-roll another sum, and remember that a derivation fix like this is invisible to the mtime/size fingerprints, so it needs `TOKEN_USAGE_CACHE_VERSION`, `GLOBAL_STATS_CACHE_VERSION` **and `CODEX_SESSION_CACHE_VERSION`** (`src/main/storage/codex-session-storage.ts`) bumped or the old numbers are served forever - that third one guards a per-session cache reused while `fileMtimeMs` still covers the file, so it is the one most easily missed.
- **Capping stored tool output:** `compactToolOutput()` / `compactSessionToolOutputs()` in `src/shared/toolOutput.ts`. Tool results cross the renderer store, disk persistence, and the web-desktop bootstrap, so cap them at ingestion and again at both storage boundaries. Do NOT add a parser-local substring limit or send the raw value to a browser client: one 67MB one-line result made the bootstrap frame 126MB and left iOS on `Loading your agents...`.
- **Timestamp for a generated file name:** `fileTimestampSlug(dateOrTimestamp?)` in `src/shared/formatters.ts` - `20260713-142530`, filesystem-safe everywhere and chronologically sortable. LOCAL time on purpose: the name is read by the human who pasted the image at that wall-clock moment, so a UTC stamp reads as the wrong hour to everyone outside UTC. Both the renderer's `saveImageToProject()` and the CLI's `image save` name files with it, so a screenshot saved by hand and the same one saved by an agent cannot come out under two different conventions.
- **Path utilities:** `truncatePath()`, `getParentDir()`, `truncateCommand()`, `isAbsolutePath()`, `getBasename()`, `joinPath()` in `src/shared/formatters.ts` (`joinPath` is the renderer-safe `path.join`: it uses whichever separator the base path speaks, so Windows and SSH-remote POSIX paths both survive)
- **Reading a zip from disk:** `readZipArchive(filePath, options?)`, `extractZipTo()`, `isUnsafeZipEntryName()` in `src/main/utils/zip-archive.ts`. Playbook import and Cue backup only need names and bytes. Pass `names` or `filter` so a manifest-only read does not inflate the rest; default caps refuse a zip bomb. `extractZipTo` walks every path component under the dest root so an intermediate symlink cannot escape. Do not bring back `adm-zip` (`extractAllTo` follows dest symlinks; no patched release).
- **Classifying a file by extension:** `getFileCategory()`, `isPreviewableFile()`, `matchesFileCategory()`, `getFileExtension()` in `src/shared/fileCategories.ts`. One table answers both "can Maestro open this?" and "which bucket is it in?" - `isPreviewableFile` is DERIVED from `getFileCategory`, so a file cannot show up under a filter pill and then refuse to open, nor be openable while invisible under every pill. Audio and video are NOT listed there; they resolve through `getMediaKind()` in `mediaTypes.ts`, which is the single source of truth for what Chromium can decode. Do NOT hand-roll another `TEXT_EXTENSIONS` set - the private copies inside `FileSearchModal` are exactly what this replaced. Distinct from the sets in `src/renderer/utils/fileExplorerIcons/shared.ts`, which exist to pick an ICON and split far more finely (lockfiles, test folders); pick by question and do not merge them.
- **Guarding a serialized JSON payload before it overwrites a file:** `assertSerializedJsonIsSafe(serialized, target)` in `src/shared/jsonUtils.ts`. The failure it stops is `JSON.stringify` returning `undefined` (for `undefined`, a function, or a symbol), which `writeFile` persists as the literal text `"undefined"` - unparseable, and a silent way to clobber a good store. It does NOT unconditionally round-trip parse: above 1MB it falls back to a balanced-delimiter check, because `JSON.stringify` cannot emit malformed JSON (it throws instead) so the parse only ever catches truncation, which cannot happen to a string still in memory. That re-parse cost ~40ms per write on the 20MB agent-runs store, paid on the Electron main thread - the same thread that answers every IPC call. Two rival copies of this guard existed: one in `atomic-json-store.ts` and a bare `JSON.parse(content)` inline in `agent-run-store.ts`. Do NOT add a third, and do NOT "harden" it back into an unconditional parse.
- **Serializing async work on one entity:** `createKeyedWriteQueue()` in `src/shared/keyedWriteQueue.ts` (re-exported from `src/main/utils/atomic-json-store.ts`, which is still the import site every main-process caller uses). Each key gets its OWN promise chain, so two callers racing on the same entity cannot interleave a read-modify-write while different keys stay concurrent, and settled entries are dropped so the backing Map stays bounded in a process that runs for days. It lives in `shared/` rather than beside `atomicWriteJson` because BOTH processes need it and the renderer cannot import a module that pulls in `fs/promises` - the same hoist, for the same reason, as `assertSerializedJsonIsSafe`. **One queue is not enough when the work crosses a process boundary.** A remote tab rename is ordered TWICE and the two queues answer different questions: `tabCallbacks.ts` orders the PROTOCOL (it is what puts `rename_tab_result` frames back on the wire in request order) while `useRemoteIntegration.ts` orders the WORK (provider metadata write, history relabel, store patch), because the server's queue stops waiting after a bounded delay so an unresponsive renderer cannot wedge a tab - and it then dispatches the next rename while the abandoned one is still running, which is exactly the lost update both queues exist to prevent. A queue held in one process cannot order work performed in another. Both key on `${sessionId}:${tabId}`; a renderer-side queue must be at MODULE scope, never a ref, since the listener is registered from an effect that can re-run and a rebuilt queue forgets the work still in flight. Do NOT hand-roll another `Map<string, Promise<void>>` chain.
- **Loading an image for canvas compositing:** `loadImageElement(src)` in `src/renderer/utils/loadImage.ts`. Wraps `new Image()` + `onload` / `onerror` in a promise and REJECTS on failure, so a caller can never draw a half-decoded element and then report success. `compositeAnnotatedImage` and `cropImageDataUrl` both use it, which is what keeps a crop and the save that follows it from disagreeing about whether the source loaded. Accepts anything an `<img src>` accepts, including `maestro-image://` store refs, and the resolved element is decoded so `naturalWidth` / `naturalHeight` are safe to read immediately. Two other loaders in the renderer look like duplicates and are NOT - do not "consolidate" them without reading why: `loadImage()` in `AchievementShareButton.tsx` resolves `null` on purpose (a missing avatar or GitHub logo falls back to a drawn placeholder rather than failing the whole share card), and `svgToPngDataUrl()` in `imageExport.ts` sets `img.decoding = 'async'` before assigning `src`, a hint this helper does not expose. Reach for `loadImageElement` when a failed load must abort the operation.
- **Saving generated image bytes to disk:** `saveImageDataUrlToDisk(dataUrl, defaultName?)` in `src/renderer/utils/imageExport.ts`. The sibling `saveImageElementToDisk()` exports an image already in the DOM; this one takes bytes produced on the fly (a page screenshot, a canvas render) that have no element to point at. Both go through the native save dialog and then `fs.writeImageFile` - `fs.writeFile` is UTF-8 and would corrupt raster bytes - and both fall back to a browser download when the dialog is unavailable (web renderer). A cancelled dialog reports `{ saved: false }` with no `error`, so a caller must not treat every non-save as a failure toast. Unlike `saveImageToProject()` it does not call `requestFileTreeRefresh`, because the user usually points it outside any workspace.
- **Screenshotting a surface exactly as it is painted:** `window.maestro.shell.capturePage(rect?)` (preload in `src/main/preload/system.ts`, handler `window:capturePage` in `src/main/ipc/handlers/system.ts`). Reach for it instead of serializing a canvas or rasterizing DOM whenever the thing on screen is composed of several render paths - the Document Graph is a `<canvas>`, a React markdown preview pane, and an HTML legend, and only the compositor knows what the user is actually looking at. Three things the handler already solves, so do not re-solve them: it captures `event.sender` rather than the main window, so a surface opened in a second window shoots itself; it floors and clamps the rect, because `getBoundingClientRect()` returns floats and Chromium answers an out-of-range region with an empty image; and it resolves `null` rather than an empty data URL when there is nothing to capture. Callers must dismiss any modal of their own and wait two animation frames first, or the shot includes the dialog that asked for it. Feature-detect (`!!window.maestro?.shell?.capturePage`) before offering the affordance - the web renderer has no bridge.
- **Strip ANSI:** `stripAnsiCodes()` in `src/shared/stringUtils.ts`
- **Reading what a TUI painted from a raw capture:** `replayTerminalScreen()`, `capturedAlternateScreen()` in `src/maestro-p/screen-replay.ts`
- **Shell escape:** `shellEscape()`, `shellEscapeArgs()`, `shellEscapeForDoubleQuotes()` in `src/main/utils/shell-escape.ts`
- **A path handed to a REMOTE shell (`cd`, `ls`, `stat` over SSH):** `shellEscapeRemotePath()` in `src/main/utils/shell-escape.ts`. `shellEscape()` single-quotes, and a single-quoted `'~/proj'` never expands: the remote looks for a directory literally named `~`, and nothing local can substitute the remote user's home (`expandTilde()` produced `/Users/<local>/git-projects`, which validated green and then did not exist on the host). A leading `~` or `$HOME` is rendered as `"$HOME/rest"`; every other path falls through to `shellEscape()` byte for byte. Every remote `cd` rides it - both SSH command builders, the terminal tab handler, `SshCommandRunner`, and all of `remote-fs.ts` - because four private copies of the rule had already drifted, and one of them defaulted to a quoted `'~'` that could not work. Do NOT hand-roll another `startsWith('~/')` branch at a new call site.
- **SSH connection options (`ssh -o`):** `resolveSshOptions()` / `buildSshOptionArgs()` / `validateSshOption()` in `src/shared/sshOptions.ts`. FOUR sites used to build this list by hand - the agent spawn builder, the Test Connection probe, `SshCommandRunner`, and the interactive terminal tab in `ipc/handlers/process.ts` - and they had already drifted: only one set `LogLevel`, and the terminal path omitted `BatchMode` entirely, so a remote could test green and then connect with a different option set when an agent actually ran on it. The merge is CASE-INSENSITIVE, which is the whole reason it is one function: OpenSSH treats keywords case-insensitively but applies `-o` FIRST-WINS, so a naive spread emitting both `-o ConnectTimeout=10` and `-o connecttimeout=45` silently keeps the 10 and the user's override does nothing. A command-line `-o` also outranks `~/.ssh/config`, so `SshRemoteConfig.sshOptions` is the ONLY way a user can change one of these defaults - that is what makes an exotic transport (a tailcat / cloudflared / Teleport `ProxyCommand`, a `ProxyJump` bastion) expressible without a field per transport. `RequestTTY` is RESERVED: it is derived per command from whether the remote agent speaks stream-json, and a forced TTY corrupts that stream, so it is a property of the command and pinning it per host silently breaks agents. The `'interactive'` context drops `BatchMode`, `RequestTTY` and `LogLevel` (the user is at the keyboard and can answer a passphrase prompt, the caller passes `-t`, and SSH's own warnings are the diagnostics someone watching a terminal needs) and an override may NOT reinstate one - adding it back re-breaks the terminal it was dropped for. Do NOT hand-roll another `-o` list; the module is import-free so the CLI bundle can use it too.
- **HTTP request from the main process:** `fetchWithTimeout(url, options?, timeoutMs?)` in `src/main/utils/fetchWithTimeout.ts`, with `isFetchTimeoutError()` and `DEFAULT_FETCH_TIMEOUT_MS`. A bare `fetch()` has no timeout, so a stalled socket hangs the caller forever: in the main process that is an IPC handler that never settles and a renderer spinner that never stops. Do NOT hand-roll `new AbortController()` + `setTimeout`, and do NOT add another local `fetchWithTimeout` - there used to be three functions with that exact name and three different signatures. It composes a caller-supplied `signal` rather than overwriting it, so passing your own cancellation still works. Callers needing extra behaviour wrap it locally (see `fetchBmadResource()` in `src/main/bmad-manager.ts`).
- **Timing out a supervised agent process:** `createIdleWatchdog({ idleMs, maxMs?, onIdle, onMax? })` in `src/main/utils/idle-watchdog.ts`, fed by the event list in `src/main/utils/agent-liveness.ts`. **The budget is SILENCE, not wall clock.** A plain `setTimeout` armed at spawn measures TOTAL duration, so an agent emitting output every second and one that died a minute in look identical to it - which is how a Group Chat participant was declared dead at ten minutes while its transcript showed 19-41 events per minute straight through the cutoff. The caller `touch()`es on every proof of life and the clock restarts; the optional `maxMs` ceiling is a SECOND, non-resettable timer, because an agent stuck in a tool loop chatters forever and can never satisfy the idle budget. What counts as proof of life is `AGENT_LIVENESS_EVENTS` (`data`, `thinking-chunk`, `tool-execution`, `usage`), and getting that list wrong silently reintroduces the wall-clock bug: for a `--print` stream-json run `StdoutHandler` emits `data` ONLY on the terminal result, so a budget armed on `data` alone is a hard deadline that never re-arms. Both the cross-agent consult router and the Group Chat router shipped exactly that. `raw-stdout` is deliberately excluded from the shared list (it fires per chunk for every process in the app) and opted into by name where wanted. **A timeout must kill before it reports**: changing only Maestro's state tells the room the turn failed while the process it gave up on goes on editing files, committing, and pushing. Kill with the FULL spawned session id - `getModeratorSessionId()` returns the per-chat PREFIX that every turn appends a timestamp to, and `ProcessManager.kill` matches exact keys, so the prefix kills nothing. Group Chat's budgets are re-armed by `setupGroupChatLivenessListener` (`src/main/process-listeners/`), which hands session ids to `noteGroupChatActivity()`; the router itself never grows an EventEmitter. Do NOT hand-roll another `setTimeout` supervisor for a spawned agent.
- **Encore Feature flags and their defaults:** `ENCORE_FEATURE_DEFAULTS`, `resolveEncoreFeatures()` in `src/shared/encoreFeatureDefaults.ts`
- **Platform detection:** `isWindows()`, `isMacOS()`, `isLinux()` in `src/shared/platformDetection.ts` (works in both processes). In renderer-only code prefer `isWindowsPlatform()`, `isMacOSPlatform()`, `isLinuxPlatform()` in `src/renderer/utils/platformUtils.ts`. Never read `process.platform` in the renderer: the `process` polyfill (`src/renderer/public/process-shim.js`) reports the sentinel `'browser'`, and `navigator.platform` is deprecated and unreliable.
- **Modifier-key display text:** `formatKey()`, `formatShortcutKeys()`, `formatMetaKey()` (symbol: `⌘` / `Ctrl`), `formatMetaKeyName()` (prose: `Command` / `Ctrl`), `formatAltKeyName()` (prose: `Option` / `Alt`) in `src/renderer/utils/shortcutFormatter.ts`. Do NOT hard-code `⌘`, `Cmd+`, `Ctrl+`, `Option`, or `Alt` in UI copy, tooltips, or setting descriptions - it renders the wrong key on the other platform. Outside the renderer (CLI, main process) use `formatShortcutKeysFor(keys, isMac)` / `formatKeyFor()` in `src/shared/shortcutKeys.ts`, which owns the key maps; the renderer file is just the platform binding over it.
- **Advertising a shortcut next to the control that fires it:** `ShortcutHint` (`variant="chip"` for an inline key-cap, `variant="row"` for a hint header pinned above a dropdown's option list) and `shortcutSuffix(keys)` (for a `title=` tooltip) in `src/renderer/components/ui/ShortcutHint.tsx`. Maestro is keyboard-first, so every clickable route to an action that ALSO has a chord should show that chord where the user is already looking; this is the one component that draws it. Four byte-identical local `ShortcutHint` definitions had accumulated across the tab-bar components before it was extracted. Two invariants: read the chord from the LIVE shortcut map (`useSettingsStore(s => s.shortcuts)` / `s.tabShortcuts`) rather than spelling a default combo into the string, or a user who rebinds sees our chord instead of theirs; and both helpers render NOTHING for an empty or missing `keys`, because an action can ship unbound and a blank key-cap advertises a combo that does nothing. When a row already has a right-aligned badge, wrap the badge and the key-cap in ONE `ml-auto` container - two auto margins on siblings split the free space and strand the badge mid-row.
- **Font-family fallback chain:** `withMonoFallback()` / `resolveSurfaceFont()` over `MONO_FALLBACK_STACK` and `SANS_FALLBACK_STACK` in `src/shared/fontStack.ts`. The font pickers store a BARE family name (`Roboto Mono`) with no generic on the end, so a name that is not installed and not web-loaded resolves to the browser's document default, which is a proportional SERIF - the whole app renders in Times. Apply the fallback where the setting becomes a CSS value, never at the setting source: the picker's `<select>` has to match its options against the raw stored name. Every surface font (terminal, chat, file preview, file editor) stores the empty string to mean "inherit the interface font", and `resolveSurfaceFont` is what that empty string means - re-deriving it and forgetting the `.trim()` renders a whitespace-only family, which resolves to nothing. Do NOT add a surface-local fallback chain; a second chain drifts the first time one of them gains a platform, and that surface then degrades to different faces than the rest of the app. `XTerminal.tsx` shipped exactly that duplicate (`ensureMonospaceFallback`) and it was folded back in. **A fallback chain is not the whole answer for the terminal**, which additionally MEASURES: a configured font can resolve perfectly and simply not be fixed-pitch, so the appended generic is never reached while xterm draws every glyph on the pitch of `W` (`Cl aude`). That half is `isFixedPitchStack()` / `resolveTerminalFontFamily()` in `src/renderer/utils/fixedPitchFont.ts` (see the entry below), and it composes with the shared chain rather than replacing it. The MAESTRO wordmark is deliberately NOT derived from any setting - it uses `WORDMARK_FONT_STACK`, because a brand mark that changes identity with the reading font is a bug.
- **Fixed-pitch font for shell text:** `resolveFixedPitchFontFamily()` / `resolveTerminalFontFamily()` / `isFixedPitchStack()` in `src/renderer/utils/fixedPitchFont.ts`, wrapped for React by `useFixedPitchFont()`. Every surface that renders shell text rides it - the terminal, the command-mode composer, and the shell command card - so a proportional UI font cannot silently break one while the others measure. It COMPOSES with the entry above rather than restating it: the fallback half is `withMonoFallback` / `MONO_FALLBACK_STACK` from `src/shared/fontStack.ts`, and this module shipped a local duplicate of that chain (`ensureMonospaceFallback`, `FIXED_PITCH_FALLBACK_STACK`) before it was folded back in. Do NOT reintroduce one. When a surface overrides the composer font this way, any decoration layered under it takes the SAME override - the mention-chip overlay is measured against the textarea's font, so a monospace textarea over a proportional overlay lands every chip off its word.
- **Saving the user's own font setup:** `captureTypographySnapshot(values)`, `typographySnapshotPatch(snapshot)`, `typographySnapshotMatches(snapshot, values)` in `src/shared/typographySnapshot.ts`, persisted under the single `typographySnapshot` setting and driven by `saveTypographySnapshot()` / `restoreTypographySnapshot()` in `src/renderer/stores/settingsThemeSlice.ts`. It is what makes the Factory Reset presets safe to try: they overwrite all six surfaces at once, so without a saved copy a user could not even LOOK at Default or Hacker without destroying a hand-tuned setup. Derive the keys from `TYPOGRAPHY_SURFACE_LIST` (`src/shared/typography.ts`), never from a hand-written list - a second list is how a newly registered surface silently stops being saved. Zoom is deliberately NOT part of a snapshot, matching `resetTypography` leaving it alone.
- **Whether a chord may be bound at all:** `findReservedShortcutCombo(keys)` over `RESERVED_SHORTCUT_COMBOS` in `src/shared/shortcutKeys.ts`. `Cmd/Ctrl+Shift+Arrow` is the OS's select-to-end inside a text field, and a Maestro binding on one wins in every input in the app, so the user loses the native behavior with no way to tell that apart from a broken text box. It is checked at BOTH ends and the second half is the one that is easy to skip: the recorder (`ShortcutsTab`) refuses a fresh binding, and `migrateShortcuts()` in `settingsStore.ts` strips one already on disk and falls back to the bundled default. A recorder-only guard leaves anyone who already bound the chord broken forever. The table is written in terms of `Meta` and comparison collapses `Ctrl`/`Command` into it, matching `eventMatchesShortcutKeys`, so one entry covers every platform - do NOT add a separate `Ctrl` row.
- **Whether a keystroke belongs to a text field (guard before `preventDefault`):** `isTextInputTarget()`, `isTextEntryTarget()` in `src/renderer/utils/messageScrollNavigation.ts`; `isEditingTextTarget()`, `isEditingTextFocused()` in `src/renderer/utils/editableTarget.ts`
- **How much of the keyboard the user has mastered:** `collectBoundShortcuts(...maps)` (denominator) and `countUsedBoundShortcuts(bound, usedIds)` (numerator) in `src/renderer/constants/keyboardMastery.ts`. Every mastery figure - the help modal's bar, the Usage Dashboard ring and its Unused Shortcuts list, the leaderboard payload, and the level-up check in `settingsStore` - runs the LIVE maps (`shortcuts`, `tabShortcuts`, `FIXED_SHORTCUTS`) through these, so they cannot disagree about the total. **A shortcut with no chord bound is excluded from both ends**: it can never be fired, so counting one in the denominator makes Keyboard Maestro unreachable forever, and listing one as "unused" tells the user to go press a chord that does not exist. The numerator is not `usedShortcuts.length` either - that list keeps ids whose binding was later cleared, which reports over 100%. Do NOT hand-roll another `Object.keys(DEFAULT_SHORTCUTS).length + ...` total; there were already two rival copies that disagreed with the surfaces rendering the number.
- **Naming the OS file manager in copy:** `getFileManagerName(platform)` / `fileManagerName()` in `src/renderer/utils/platformUtils.ts` return the bare noun (`Finder` / `Explorer` / `File Manager`), and `getRevealLabel()` / `getOpenInLabel()` are built from it. Do NOT hard-code `Finder` in a hint, tooltip, or menu label - it reads wrong on Windows, and the failure is invisible on a Mac dev machine. Same class of bug as hard-coding `⌘`.
- **Touch primitives:** `isCoarsePointer()`, `triggerHaptic()`, `supportsHaptics()`, `HAPTIC_PATTERNS`, `GESTURE_THRESHOLDS`, `MIN_TOUCH_TARGET` in `src/renderer/utils/touch.ts` (do NOT re-derive `navigator.vibrate` calls or `matchMedia('(pointer: coarse)')` queries)
- **Long-press gesture:** `useLongPress()` in `src/renderer/hooks/utils/useLongPress.ts` (scroll-aware touch long-press that opens right-click affordances like context menus and tab overlays; do NOT hand-roll `setTimeout` + `touchmove`)
- **Color math and contrast:** `readableTextOn()`, `isReadableOn()`, `contrastRatio()`, `relativeLuminance()`, `blendColors()`, `transparentize()`, `adjustBrightness()`, `hexToRgb()` in `src/shared/colorContrast.ts`. Whenever a foreground and its background are BOTH derived from theme colors (diagram fills, chart labels, badges, generated SVG), run the foreground through `readableTextOn(preferred, backgrounds)` so a theme whose accent sits near its text color can't paint near-identical colors on top of each other. It returns the preferred color untouched when it already clears WCAG AA and nudges the theme's own color otherwise, so nothing snaps to hard-coded black/white.
- **Agent display name:** `getAgentDisplayName()` in `src/shared/agentMetadata.ts`
- **Which providers a user can pick:** `AGENT_PICKER_META` / `PICKABLE_AGENT_IDS` / `getAgentPickerMeta()` in `src/shared/agentMetadata.ts`. ONE registry feeds all three provider pickers: the New Agent modal (`SUPPORTED_AGENTS` re-exports it), the New Agent Wizard's tile strip (`AGENT_TILES` is derived from it), and the Group Chat moderator dropdown (the same tiles, filtered by what detection found). Do NOT hand-write a fourth list of agent ids for a new picker: the three used to be written by hand, and Grok and Qwen3 Coder shipped selectable in the New Agent modal while being absent from the wizard and un-pickable as a moderator. The record is keyed by `AgentId`, so a new id fails to compile until it is either given metadata or explicitly set to `null` (internal or unshipped). **Order is derived, not authored:** `PICKABLE_AGENT_IDS` sorts by display name, so every picker shows one predictable alphabetical list and a new entry can go anywhere in the record. Do NOT re-sort the record by hand to move a provider, and do NOT default a picker to the first entry - that hands a fresh Group Chat to whichever beta provider sorts first. Auto-selection reads `AGENT_AUTOSELECT_ORDER` (a preference list) and picks the first entry the user actually has installed. Adding a provider also means a `case` in `AgentLogo` and a glyph in `AGENT_ICONS`, or the tile draws a blank fallback ring. What a picker SHOWS is narrowed again at render time by detection - see `filterToAvailableProviders` below - so a provider missing from a picker may simply not be installed on that machine. Full checklist: [PROVIDER-SUPPORT.md → Step 2.6](PROVIDER-SUPPORT.md#step-26-register-the-provider-in-the-pickers).
- **Which account dirs exist for a provider (main):** `getProviderAccountDirs(toolType, options?)` in `src/main/agents/provider-account-dirs.ts`
- **Reading a Codex account's credentials off disk:** `readCodexAuth()`, `codexAuthHeaders()`, `extractEmailFromJwt()` in `src/main/agents/codex-auth.ts`. `CODEX_HOME/auth.json` is the one place a Codex account's identity lives, and both the quota sampler and the reset-credit primitives need it. A second copy drifts first on the HEADER: a request without `ChatGPT-Account-Id` answers for the WRONG account when the login holds several workspaces, and an empty one is rejected rather than falling back - so it is omitted, never sent blank. The two failure kinds stay apart because their remedies differ (`missing_auth` = never logged in, `unauthenticated` = present but no usable token). The JWT email is a LABEL and its signature is deliberately unverified.
- **Codex rate-limit reset credits:** spend rules in `src/shared/codexResetCredits.ts` (`describeCreditSpend`, `shouldAutoSpendCredit`, `nextCreditToSpend`, `describeCreditExpiry`), the two primitives in `src/main/agents/codex-reset-credits.ts` (`fetchCodexResetCredits` READs, `consumeCodexResetCredit` WRITEs), renderer state in `src/renderer/stores/codexResetCreditsStore.ts`, automation in `src/renderer/services/codexAutoReset.ts`. **The count is free** - `rate_limit_reset_credits` already rides the `/wham/usage` payload, so `CodexUsageSnapshot.resetCredits` costs no extra request and only a surface rendering the LIST pays for the detail read. **THE trap: `available` and `applicable` are different questions.** `available` is inventory; `applicable` is how many would take effect RIGHT NOW, and the API returns 0 there whenever nothing is consumed enough to benefit (a real account read `2` and `0`). A credit spent then is simply gone, so every surface goes through `describeCreditSpend()` rather than pairing `available` with an unconditional button, and an ABSENT `applicable` stays `undefined` rather than becoming 0 - unknown and zero drive different verdicts. **Three wire-format traps in `consume`:** `redeem_request_id` is REQUIRED and IS the idempotency key, so it belongs to one logical SPEND and must never be minted per HTTP attempt inside a retry loop; failure arrives as **HTTP 200** (`{"code":"no_credit","windows_reset":0}`), so `response.ok` alone reports every refusal as success; and `windows_reset > 0` is the only proof anything happened. The AUTOMATIC path is strictly narrower than the button - `shouldAutoSpendCredit` refuses `wasteful` AND `unknown`, plus SSH-backed agents (those credits belong to THIS machine's account) - and fires at most once per outage, keyed at MODULE scope because the listener is re-registered whenever its effect re-runs. Do NOT call `consumeCodexResetCredit` from a refresh, sweep, or retry default: every path to it is an explicit click or an explicitly enabled per-agent toggle.
- **SSH remote lookup:** `getSshRemoteById()` in `src/main/stores/getters.ts`
- **Deferred main-process store persistence:** `deferStoreWrites()`, `flushPendingSessionWrites()` in `src/main/stores/deferred-writes.ts` / `src/main/stores/instances.ts`
- **Toast notifications:** `notifyToast({ color, title, message, dismissible? })` in `src/renderer/stores/notificationStore.ts`. Use for async results, errors, and persistent/dismissable messages. Same five-color design language as Center Flash: `green | yellow | orange | red | theme` (default `theme`). Set `dismissible: true` (or pass `--dismissible` from `maestro-cli notify toast`) when the user MUST acknowledge - disables auto-dismiss, requires click to close, and emphasizes the X button. Cannot combine `dismissible` with `duration`/`--timeout`. External CLI cap: 60 seconds (use `--dismissible` for sticky). **Click actions** (data-driven, survive the IPC bridge): pass `clickAction` for what should happen when the toast body is clicked, or use the legacy `sessionId`/`tabId` fields for plain agent jump. The shape and its validator (`parseToastClickAction`) are canonical in `src/shared/toastClickAction.ts` and aliased by every producer (web-server types, preload, `global.d.ts`, `cue-notify-bridge`); the renderer dispatches every kind through `dispatchToastClickAction()` in `src/renderer/services/toastClickActions.ts`. Kinds: `jump-session` (AI tab), `open-file` (File Preview tab), `open-terminal` (focus an existing terminal tab by id or name; omit `tabRef` for the agent's active one), `open-browser` (focus an in-app browser tab by `tabId`, or open `url` in a new one), `open-url` (system browser). Adding a kind is a change in exactly those two files. From the CLI: `--agent` (+ optional `--tab`), `--open-file <path>`, `--open-terminal [tab]`, `--open-browser <url>`, `--open-browser-tab <id>` (all requiring `--agent`), `--open-url <url>` - mutually exclusive with each other. `--action-url` / `--action-label` render an inline link button beneath the message and are independent of the body click. **Source-agent label:** pass `sourceAgent` (CLI `--source-agent <label>`) to stamp which agent/pipeline fired the toast in the header strip. It's store-independent, so it shows even when the agent isn't loaded in the Left Bar (the name resolved from `--agent`/`sessionId` only renders when that agent is in the desktop store) - use it for cron/watchdog toasts. The explicit label wins over the resolved name for display; pair it with `--agent` to also get click-to-jump. Do NOT pass renderer-only callbacks across the bridge - use `clickAction` instead.
- **What a toast click does:** `ToastClickAction` + `parseToastClickAction()` in `src/shared/toastClickAction.ts`, dispatched by `dispatchToastClickAction()` in `src/renderer/services/toastClickActions.ts`. The shape is data-only so it survives the IPC bridge, and every producer (web-server types, preload, `global.d.ts`, `cue-notify-bridge`) aliases the canonical type rather than restating it. Adding a click kind is a change in exactly those two files - a producer that declares its own union compiles fine and then hands the renderer a kind nothing dispatches, so the toast silently does nothing when clicked.
- **Center flash (rapid acks):** `notifyCenterFlash({ message, color, detail?, duration? })` in `src/renderer/stores/centerFlashStore.ts`; clipboard helper `flashCopiedToClipboard()` in `src/renderer/utils/flashCopiedToClipboard.ts`. Use for momentary "I did the thing" confirmations of user-initiated actions. Five-color design language: `green | yellow | orange | red | theme` - default `theme` matches the active Maestro theme. External integrations can fire flashes via `maestro-cli notify flash <message> --color <color>`. Do NOT roll your own center-screen overlay, useState+setTimeout flash, add a sixth color, or use a Toast for clipboard acks. Single visible flash at a time, themed frosted-glass card mounted once in `App.tsx`. Full decision rules, color palette, and design language: [UI-PATTERNS.md → Center Flash System](docs/agent-guides/UI-PATTERNS.md#center-flash-system-rapid-temporary-notifications).
- **Opening a modal / dashboard by name:** `UI_SURFACES` + `resolveUiSurface()` / `resolveUiSurfaceTab()` / `describeSurfaceAccess()` in `src/shared/uiSurfaces.ts`, dispatched by `openUiSurface()` in `src/renderer/utils/openUiSurface.ts`. One registry serves `maestro-cli open <surface> [--tab]`, the main-process `open_modal` validator, and the renderer dispatcher, and it carries the discovery hints (hotkey id, command-palette text, click target) so an agent can teach the manual path while opening the pane. Adding a modal means adding one entry, not touching four files - and if it has tabs, list them there so `--tab` can deep-link.
- **Whether a modal takes the window over:** `DESTINATION_MODALS` + `DESTINATION_SHORTCUT_IDS` + `registerExternalDestination()` in `src/renderer/stores/modalStore.ts`. Full-window views you GO to (Settings, Usage Dashboard, Director's Notes, Symphony, Cue, Marketplace, Process Monitor, System Logs, Agent Sessions, Memory, Document Graph) close each other on open, so what you see is decided by what you just asked for rather than by each surface's fixed rank in `MODAL_PRIORITIES`. Before that, the Usage Dashboard opened BEHIND Director's Notes and a main-panel destination opened under an overlay changed nothing on screen - both read as a dead keystroke. Dialogs that answer a question about the surface beneath them still layer normally, so do NOT register one as a destination.
- **Toggling the unread filters:** `toggleAllUnreadFilters()` / `toggleTabUnreadFilter()` in `src/renderer/services/unreadFilters.ts`, with `areUnreadFiltersActive()` beside them. The agent filter and the tab filter stay independent, but one press drives both, and a half-filtered view COMPLETES rather than clears. The tab side routes through `toggleTabUnreadFilter` rather than `setShowUnreadOnly` so the pre-filter tab is still saved on the way in and restored on the way out - that save/restore dance used to live inline in `useSessionLifecycle`, where the three entry points could disagree about it.
- **Scheduled Tasks (clock-driven Cue subs):** types and pure helpers in `src/shared/cue/scheduled-tasks.ts`, filesystem work in `src/main/cue/cue-scheduled-tasks.ts`. `maestro-cli cue schedule` and the Cue modal's Scheduled Tasks tab (via `cue:*ScheduledTask*` IPC) both go through that one module, so they cannot write different YAML for the same task. Do NOT hand-roll another cue.yaml writer.
- **Whether two subscriptions are the same visual trigger:** `triggerGroupKey(sub)` in `src/shared/cue/trigger-group-key.ts`. The pipeline serializer writes fan-out to mixed or command targets as SEVERAL parallel subscriptions that each re-carry the whole trigger config, so "one trigger" is a derived identity, not a stored id. Three surfaces need that identity and must agree: `yamlToPipeline` collapses matching subs onto one trigger node, `cue-engine` fires every sibling when the user clicks Play (so a manual run matches a scheduled tick), and `SessionsTable` picks one representative per group for Run Now. They previously held three hand-mirrored copies, and the two runtime copies had already drifted from the editor's - they omitted `retrigger_on_comments` / `max_notifications`, so Play fired subs a real tick would have treated as separate triggers. Adding a new event-config field means adding it HERE, once: leave it out and two subs that differ only in that field silently collapse into one. Do NOT re-inline a JSON.stringify of the trigger fields at a new call site.
- **How big a pipeline is on the canvas:** `pipelineCardBounds(nodes, opts?)`, plus `nodeFootprintWidth()`, `estimateNodeWidth()` and `NODE_CHROME_OVERHANG` in `src/renderer/components/CuePipelineEditor/utils/nodeFootprint.ts`. Three surfaces need a pipeline's on-screen rect and must agree: the renderer that sizes the translucent group card (`convertToReactFlowNodes`), the drag handler that stops two dropped pipelines landing on each other (`resolveNonOverlappingPipelineOffset`), and the masonry packer that deals cards into columns (`arrangePipelineGroups`). They used to disagree - only the packer sized a node from its text, while the other two assumed a flat `NODE_BG_WIDTH` - so a node with a long name rendered outside the card that was supposed to contain it. A node is bigger than the box ReactFlow positions in two ways: its width is text-driven (`width: max-content`), which `estimateNodeWidth` derives from the same text the node components render and errs WIDE on purpose; and its chrome hangs OUTSIDE that box, since handles are centred on the borders and badges are pinned past a corner, which `NODE_CHROME_OVERHANG` allows for. Do NOT size a card from `NODE_BG_WIDTH` (it is a FLOOR, not a maximum) and do NOT fold the overhang into `PIPELINE_GROUP_PADDING`.
- **Which pipelines belong to an agent:** `pipelinesForSession(sessionId, pipelines, graphSessions)`, plus `pipelineInvolvesSession()` and `pipelineNamesDeclaredBySession()` in `src/renderer/components/CuePipelineEditor/utils/pipelineMembership.ts`. "Belongs" has two independent senses and the Cue dashboard needs both: PARTICIPATION (an agent node bound to the session, or a command node whose `owningSessionId` is the session) and DECLARATION (the pipeline is defined in that agent's own `cue.yaml`, even when no node points back at it - a fan-out that only dispatches to other agents, or an `action: command` pipeline that is just a trigger plus a shell step). The predecessor, `getPipelineColorForAgent()`, answered a COLOR question and checked agent nodes only, which broke twice over: a command-only pipeline belonged to nobody, so the Sessions table printed a dash and "View in Graph" dropped the user into every pipeline on the machine; and because two pipelines can legitimately share a color, pairing a color list back to names dropped one and mislabeled the other. Membership is resolved by pipeline identity, never by color - `PipelineDot` is keyed on `p.id` for the same reason. Declaration matches on `getPipelineKey(sub)` because names are the grouping key `subscriptionsToPipelines` builds with, so they equal `CuePipeline.name` exactly. Do NOT reintroduce a color-based lookup, and do NOT filter agent nodes inline at a new call site.
- **What a group chat does with a message it cannot send yet:** the pure rules in `src/shared/groupChatQueueModel.ts` (`mustQueue`, `canDrain`, `enqueueMessage`, `completeItem`, `failItem`, `failHead`, `requeueFailedAtFront`, `resumeQueue`, `removeItem`, `reorderItem`, `parseQueueState`), driven by `src/main/group-chat/group-chat-queue.ts`. **The queue is owned by MAIN, not by a client.** It used to be a zustand array in the renderer, so every client had its own: a message queued on a phone was invisible to the desktop, died with the browser tab holding it, and was delivered only if that one client happened to observe the moderator going idle. Main is also the only party that can decide whether a message is sent or queued - a client decides from a copy that is already stale, and a stale copy sends directly while items are waiting, putting the NEWEST message in front of older ones, which is the one thing a queue exists to prevent. The invariant the whole module holds: a message the user typed is delivered, or it is still queued with the chat paused and the user told why; there is no third outcome. So a failed send KEEPS the item, marks it, pauses, and posts a system message, and never retries on its own - a cause that does not clear (the Encore Feature off, a missing binary) would otherwise re-fire on every idle forever. Three traps the model encodes so no caller re-derives them: completion and failure are recorded BY ID rather than by position (the send is awaited and the queue is editable while it is in flight, so "drop whatever is first now" discards a message the moderator never received); `enqueueMessage` RESUMES rather than merely un-pausing, because `canDrain` refuses a failed head and a queue that reports itself running while sending nothing is a worse stall than a visible pause; and `parseQueueState` THROWS on a malformed file rather than degrading to empty, so the caller can quarantine the bytes instead of letting the next save erase them. The renderer holds only a MIRROR - `groupChatQueues` in `groupChatStore`, keyed by chat id and written from nothing but the `groupChat:queueState` broadcast and the `getQueue` read a chat performs when it opens, since a client can have been asleep, reloaded, or never seen the chat before. It has NO drain: a renderer-side drain only runs while that client is awake and watching, so a phone that slept left its messages queued forever and two clients that both saw idle each sent the same item. `GroupChatInput` adapts the mirrored items to the `QueuedItem` shape `QueuedItemsList` already speaks, so main never learns a renderer type, and it renders the pause: a paused queue sends nothing, so it has to say so and offer Resume rather than leaving messages sitting there with no explanation and no control. Do NOT add a second queue in a renderer store, and do NOT decide send-vs-queue on the client.
- **How much work happened in a group chat:** `computeGroupChatActivity(entries)` in `src/shared/groupChatActivity.ts`, fed by the per-turn measurements `src/main/group-chat/group-chat-turn-metrics.ts` stamps onto each history entry. **Never present the span between a chat's first and last message as effort** - a group chat is a ROOM, not a task, so a chat used twice a week reports hundreds of hours without anyone doing anything (a real one read `481h 34m` for about five hours of work). Working time is the UNION of the busy intervals, because participants run in PARALLEL: three agents working the same ten minutes is ten minutes of chat time, not thirty, and summing per-agent durations produces a number larger than the chat's own age. Entries written before per-turn timing existed carry no `elapsedTimeMs` and degenerate to points, so `GROUP_CHAT_ACTIVITY_STITCH_MS` treats turns closer than five minutes as one block - without it an older chat reports `0m`, which is a worse lie than the span it replaced. Turn duration is measured with `beginSleepAwareSpan()`, never a bare `Date.now()` delta. `turnsWithTokens` / `turnsWithCost` are coverage counts: render a dash rather than a zero when they are 0, since a chat whose turns reported no usage is UNKNOWN, not free. Both the Group Chat Info overlay and the HTML export call the one function, so they cannot disagree about how much work a chat did.
- **How big a tab's conversation is and how long it ran:** `computeTabConversationStats()`, `formatConversationDuration()` in `src/shared/tabConversationStats.ts`
- **Whether an agent is drawn in the Left Bar:** `sessionMatchesFilter()` and `passesUnreadFilter()` in `src/renderer/utils/sidebarMembership.ts`. "Visible" means drawn ANYWHERE in the scrollable list; scroll position is irrelevant. Both the render path (`useSessionCategories`) and `Cmd+[` / `Cmd+]` (`useCycleSession`) call these, because a cycle that disagrees with the list about membership moves the cursor onto an agent that is not on screen, which reads as the shortcut jumping at random. Adding a match rule here fixes both at once. The "does this agent need attention?" half of that answer lives in `sessionNeedsAttention()` / `sessionOrChildrenNeedAttention()` in `src/renderer/utils/sessionAttention.ts`, because the bell badge, the collapsed rail (`SkinnySidebar`), and the jump-badge projection (`useSortedSessions`) ask it WITHOUT a filter context - so a new attention rule (unread, busy, **error**, Auto Run batch, stuck outage) goes there and every surface picks it up at once. Errored agents were invisible in the unread filter for exactly this reason: four rival copies of the predicate, and only one of them knew about `state === 'error'`. The inputs are shared state (`uiStore.sessionFilter`, `uiStore.showArchivedGroupChats`) rather than local `useState` for the same reason - see [STATE-PATTERNS.md → uiStore](docs/agent-guides/STATE-PATTERNS.md#uistore). Note that `orderGroupChatsForDisplay()` drops archived chats ITSELF, so a caller that has already decided archived chats are on screen must pass `{ includeArchived: true }` rather than pre-filtering, or they are removed again on the way through.
- **Whether an agent is answering a consult right now:** `useSessionIsBeingConsulted(sessionId)` for one row, `useConsultedSessionIds()` / `selectConsultedSessionIds(requests)` for a surface that draws many rows in one pass, all in `src/renderer/stores/crossAgentInFlightStore.ts`. A cross-agent `@mention` runs under a synthetic `cross-agent-<requestId>` process id and writes into a HIDDEN tab, so by design it never touches the consulted agent's `state`, unread flags, or attention badge - background work must not disturb the conversation the user has open with that agent. The cost was that a consulted agent sat there drawing a green "Ready" dot for the whole time it was working, and a Claude agent the user had never opened drew the dim "No active Claude session" hollow dot, because a fresh consult tab has no provider session until the answer lands. This store is the one signal that leaks back out, and it is deliberately narrow: enough for a busy dot, never enough to mark the agent as needing attention - do NOT add a consult check to `sessionNeedsAttention()`, which is what lights badges the user has no chip to clear. Three surfaces read it and must agree: the Left Bar row dot (`getEnhancedStatusColor`, which also suppresses `isDisconnected`), the collapsed rail (`SkinnySidebar`), and the worktree pill strip (`CollapsedSessionPill`). Ranking is the part that is easy to get wrong: ABOVE the unbound-Claude hollow dot (an unopened Claude agent is the COMMON consult target, so the hollow dot would mask exactly the case this exists to show) and BELOW the agent's own `state === 'busy'` and any stuck outage, which draw the same dot but carry a more specific label. Do NOT hand-roll another loop over `requests` at a new call site: the per-row hook returns a boolean so a row re-renders only when the answer flips, and the set form is memoized on the `requests` identity, which changes only when a consult starts or finishes.
- **Taking the user to an agent:** `jumpToAgent(sessionId, { tabId? })`, `revealAgentInSidebar(session)`, `openAgentSettings(session)` in `src/renderer/services/agentNavigation.ts`
- **Session lookup:** `selectActiveSession()`, `selectSessionById()` in `src/renderer/stores/sessionStore.ts`; `useActiveSession()` hook in `src/renderer/hooks/session/useActiveSession.ts`
- **Which agent this client is looking at:** `persistActiveSessionId()` / `readPersistedActiveSessionId()` in `src/renderer/utils/activeSessionPersistence.ts`. Do NOT call `window.maestro.sessions.getActiveSessionId()` directly to restore focus: the desktop and every web-desktop browser tab share that one pointer, and a browser reloads on every refocus, so reading it landed the user on whatever the DESKTOP had focused rather than the agent they were working in. Web-desktop answers it from a ladder - `sessionStorage` (this browser TAB's own choice), then `localStorage` (the last choice made in this browser), then the shared value - because two tabs share an origin, so a localStorage-only answer just moves the bleed one level down. Writing is unchanged on both - the shared pointer is what plugin `session.activated` and the CLI's current-agent answer read, so a client that went quiet there would trade one bug for another.
- **Telling the other clients an agent appeared or went away:** `sessions:lifecycleSync`, emitted by `sessions:setMany` / `sessions:setAll` in `src/main/ipc/handlers/persistence.ts` and applied by `useSessionLifecycleSync` (`src/renderer/hooks/session/useSessionLifecycleSync.ts`). Every client - each desktop window, each browser tab - holds its OWN session tree and flushes it into one store, so without the push a close never propagated and the peer's stale copy wrote the agent straight back. Four things keep it honest: main tombstones a closed id so a flush already in flight cannot resurrect it (the newest 1000, bounded by COUNT rather than age - a suspended browser tab can be away for hours, and ids are never reused, so an old tombstone has nothing to block but a stale write; a client that missed a close and outlived its tombstone reloads on reconnect anyway, since the web bridge has no replay), only ADDITIONS travel from `setAll` (a bootstrap snapshot predates anything a peer created, so an absent id there is ignorance, not a close), the hook waits for the startup load before applying (the load ends in one `setSessions` that would overwrite the delta), and deltas apply strictly in ARRIVAL ORDER through one promise chain (an add still restoring when the close for the same agent arrives makes the close look like it names an agent this client never had). It is lifecycle only - tab contents, read-state and queued messages remain last-writer-wins.
- **Session mutation:** `updateSessionWith(sessionId, updater)` for a whole agent, plus one single-tab updater per tab type in `src/renderer/stores/sessionStore.ts` - `updateAiTab(sessionId, tabId, updater)`, `updateFileTab(sessionId, tabId, updater)`, `updateBrowserTab(sessionId, tabId, updater)`. Do NOT hand-roll `setSessions(prev => prev.map(...))` for a single-agent or single-tab patch; reach for the matching helper instead.
- **Moving an agent to another directory:** `withWorkingDirectory(session, dir)`, `workingDirectoryChangeBlocker(session)`, `isSameDirectory(a, b)`, `rebasePathOntoRoot()` in `src/renderer/utils/agentWorkingDirectory.ts`. An agent's directory is FIVE fields, not one: `cwd`, `fullPath`, `shellCwd`, `projectRoot`, and an Auto Run folder living under the old root all move together, so moving `cwd` alone leaves the file tree, the shell, provider session storage, and the playbook folder pointed at the previous project. `workingDirectoryChangeBlocker` is the single reason a move is refused (a live agent process - the PTY's cwd is fixed at spawn time - or an unvalidated SSH path); ask it rather than re-deriving the guard, and remember a trailing slash is NOT a move (`isSameDirectory` normalizes), so a no-op edit must not tear down the tree. Every surface that relocates an agent rides it: the Edit Agent dialog, the web bridge's `update_session_cwd`, and `maestro-cli update-agent --cwd`.
- **Whether a PR is being opened right now:** `usePRCreationActive(worktreePath)` / `startPRCreation(target)` in `src/renderer/stores/prCreationStore.ts`, settled by `usePRCreationNotifier`. `gh pr create` can take a minute, so the request lives in the store rather than in `CreatePRModal`, which the user is free to close while it works. A second `startPRCreation` on the same repo re-attaches instead of opening a duplicate PR, `usePRCreationActive` is what the menus badge with, and the notifier reports the settlement exactly once - toast plus history entry - from a host that stays mounted. Do NOT `await window.maestro.git.createPR` inside a component: a close would throw away the PR's url and its error with it. Same shape as `gitCommandRunStore` for pull/push.
- **Ending a turn with no process exit:** `settleTabThinkingState(session, tabId)` in `src/renderer/utils/tabHelpers` - the inverse of `markTabRunningQueuedItem`. A tab's pulsing dot and its row on the Thinking pill are driven by `tab.state === 'busy'` plus the session's `state`/`busySource`/`thinkingStartTime`, and normally ONLY the process-exit listener clears them. Any path that ends a turn without a process exiting has to settle that state itself, or the tab pulses forever and the pill keeps counting elapsed time for work nobody is doing - `cancelRetry` (`src/renderer/stores/retryStore.ts`) is the live case, where a `scheduled` auto-retry's resend never spawned. Do NOT hand-roll the patch: it also recomputes the SESSION from whatever is still running (another busy tab, or an entry in `orphanedThinkingTabs` - a tab the user closed mid-turn, kept solely so the pill keeps counting it, which settling retires outright), and it deliberately leaves `state === 'error'` alone, since that is the blocking modal's state and settling a tab must not dismiss an error the user still has to act on. Only settle a tab with nothing behind it: an `in-flight` resend IS a live process, and its busy state belongs to the exit listener.
- **Leaving inline wizard mode:** `flattenWizardIntoTab(tab, { summary? })` in `src/renderer/utils/tabHelpers/index.ts`. The inline wizard (`/wizard`) keeps its conversation in `tab.wizardState.conversationHistory` and renders it through `WizardConversationView`, a store entirely separate from `tab.logs` / `TerminalOutput`. So dropping `wizardState` drops the whole wizard conversation, and the provider session handle with it, leaving a tab that looks empty - which is what shipped for every exit except a successful completion. Do NOT set `wizardState: undefined` by hand anywhere; call this instead. It appends the transcript to `tab.logs`, promotes `wizardState.agentSessionId` onto `tab.agentSessionId` so the plain tab can keep talking to the same provider context, appends an optional closing `summary` entry, and only then clears the wizard. It is idempotent (entries are matched by their `wizard-` prefixed ids), which matters because the wizard sync effect in `useWizardHandlers.ts` can race a caller that already flattened. Deliberately NOT wired to closing the wizard TAB: that already warns the user, and the tab itself is going away.
- **Naming a tab from the user's message:** `requestTabAutoName()`, `collectNamingPrompt()`, `requestWizardTabAutoName()` in `src/renderer/services/tabAutoNaming.ts`
- **Which provider account an agent runs as:** `resolveAgentProfile()` / `resolveAgentAccountKey()` / `effectiveAgentCustomEnvVars()` / `resolveAgentBillingCredential()` over `PROVIDER_PROFILE_CONFIGS` in `src/shared/providerProfiles.ts`, indexed for the renderer by `useProviderProfiles(sessions)` in `src/renderer/hooks/stats/useProviderProfiles.ts`. An account is whichever `CLAUDE_CONFIG_DIR` / `CODEX_HOME` that agent resolves to, so it is derived from the agent's effective environment rather than stored on the agent; a provider that keeps one credential store collapses to a single profile named after the provider. It is what the Usage Dashboard's provider filter, the per-card account badge, and the `Provider` sort all read, and an agent whose account has not resolved is ABSENT from `profileKeyBySessionId` rather than being given a placeholder key - so it drops out of an explicit account filter instead of being lumped into whichever one was picked. **Env vars REPLACE, they do not layer:** `applyAgentConfigOverrides()` hands a process the agent's own `customEnvVars` OR the provider-level set, never both, so `effectiveAgentCustomEnvVars()` is what any viewer or sampler must resolve - a merge of the two describes a process nobody is running, and the startup usage sampler probed a `CLAUDE_CONFIG_DIR` the agent never receives. An API key, gateway, or cloud provider OUTRANKS the config dir's login (`resolveAgentBillingCredential`): a keyed agent bills nobody's plan, so it gets its own profile and drops out of the quota panels' counts rather than eating a plan's bars. An SSH-remote agent is filed as `account @ host` for the same reason - its config dir is a path on the REMOTE host holding that host's login, which can be a different account from this machine's same-named dir.
- **Per-agent git actions:** `useGitAgentActions(session)` in `src/renderer/hooks/git/useGitAgentActions.ts` - the shared View Git Log / View Git Diff / Pull / Push / Change Branch / Create PR / Configure Worktrees set behind ALL THREE surfaces that offer it: the header branch pill dropdown, the Left Bar right-click menu, and the command palette (`buildGitWorktreeCommands`). Also exports `resolveGitCwd()` / `resolveGitSshRemoteId()`. Adding a git action means extending this hook and surfacing it in all three, not re-deriving cwd + SSH id + modal calls. See [REMAINING-SYSTEMS.md](docs/agent-guides/REMAINING-SYSTEMS.md#usegitagentactions-srcrendererhooksgitusegitagentactionsts).
- **What an unnamed tab is called:** `getTabDisplayName(tab)` in `src/renderer/utils/tabHelpers`, over `formatSessionId(agentSessionId)` for the id label itself (`8535E0E3`, `SES_4BCD`, `THR_ABC1`). Do NOT hand-roll `agentSessionId.substring(0, 8).toUpperCase()`: it agrees with the strip on a UUID and disagrees on every other provider, so `useAgentExitListener` was stamping history entries `THREAD_A` for a Codex tab the strip was drawing as `THR_ABC1`. Before writing a RECORDED name back onto a tab, ask `isSessionIdLabel(name, agentSessionId)` - surfaces that capture a display name (a history entry's `sessionName`, the session-origins store) capture whatever was on screen, so an unnamed tab records its own fallback, and restoring that as `tab.name` renders identically while permanently opting the tab out of ever being auto-named. It compares against the formatter rather than matching a hex shape, because only one of the four id forms is 8 hex characters and a regex silently lets the other three through.
- **Where a closed tab's name still exists:** `HistoryEntry.sessionName`, and in practice nowhere else. The Claude session-origins store that `handleResumeSession` falls back to is Claude-only AND written only by an exit synopsis, so for every other provider - and any session that never produced one - the history record is the last surviving copy. Every surface that reopens a past session must therefore pass the recorded name into `handleResumeSession`'s THIRD argument (the second is `providedMessages`); the four that do are the History pill, the list's `Cmd+Enter`, the detail modal's Resume, and Director's Notes. Dropping it brings the conversation back labeled with the id octet the user was not looking at.
- **Whether an AI tab is drawn in the tab strip:** `isAiTabHidden(tab)` in `src/renderer/utils/unifiedTabOrderUtils.ts`, and `visibleAiTabs(tabs)` beside it for the list form (returns the input by reference when nothing is hidden, so callers can memoize on identity). Any surface that ENUMERATES or COUNTS an agent's tabs runs the list through `visibleAiTabs` first - the tab switcher's Open Tabs, cross-tab search, the merge target list, the palette's "Close all N tabs", the Usage Dashboard's per-agent tab count. A bulk close does too (`excludePreservedRefs` in `src/renderer/hooks/tabs/internal/unifiedCloseHelpers.ts`), because the repaired order deliberately keeps a hidden ref and closing it would destroy the consult transcript and its resume id without the user ever seeing a chip. An inbound `@mention` spawns a consult tab with `hidden: true` that lives in `session.aiTabs` and keeps its `unifiedTabOrder` ref (that ref is what restores its position on reveal) but has no chip. So `aiTabs.length` is NOT the number of tabs on screen, and any code answering "does a tab survive this close?", "which tab is to the left?", or "what do we render when `activeTabId` is stale?" has to filter first - `buildUnifiedTabs`, `getNavigableTabs`, `getActiveTab`, and `closeTab` all do. Counting a hidden tab as a survivor is what let closing the last visible chip skip the replacement-chat branch and activate a consult instead: empty strip, a conversation the user never opened, and no chip to click back from. **Unread is the other half of the same rule:** a consult is answered in the BACKGROUND, so it must raise no unread anywhere, and `hasUnreadVisibleTab(tabs)` (same module, re-exported from `tabHelpers`) is the one predicate behind every unread MARK - `sessionNeedsAttention`, the Left Bar row dot, the collapsed rail, `SkinnySidebar`, and the tab strip's bell. Do NOT write `aiTabs.some((t) => t.hasUnread)`: a hidden tab has no chip, so a badge lit by one can never be cleared - the user opens the agent, every visible tab is already read, and the dot stays lit forever. The batched updater refuses to SET `hasUnread` on a hidden tab for the same reason (clearing one is still allowed), and `findNextUnreadSession` never stops on one, since the jump path reveals whatever it lands on and would turn the background consult into a foreground tab.
- **Focus an AI tab:** `aiTabFocusFields(tabId?)` in `src/renderer/utils/tabHelpers` - spread into a session patch (`{ ...s, ...aiTabFocusFields(tabId) }`) to land on an AI tab. It clears `activeFileTabId`/`activeTerminalTabId`/`activeBrowserTabId` and sets `inputMode: 'ai'`. Do NOT hand-roll the literal: those non-AI tab types outrank the AI tab in the render precedence, so omitting even one leaves the previous view on screen.
- **Focus a file tab:** `fileTabFocusFields(tabId)` in `src/renderer/utils/tabHelpers` - the file-tab counterpart to the above. Clears the terminal and browser selections (both outrank the file tab) and sets `inputMode: 'ai'`.
- **Audio/video playback:** media NEVER becomes a file preview tab. `handleOpenFileTab()` (`src/renderer/hooks/tabs/internal/useFilePreviewTabHandlers.ts`) is the single choke point every open path funnels through, and it diverts playable media to `useMediaPlaybackStore.openMedia()` (or `enqueueMedia()` when the caller passes `mediaMode: 'queue'`) before a tab can be created. Any path that opens SEVERAL files must pass `mediaMode: 'queue'` for every media file after the first - see `openFilesInOrder()` in `useFileContextMenu.ts` - or each open steals the player from the one before and only the last survives. The only surface media appears on is `FloatingMediaPlayer` - there is no docked/in-panel placement, so do NOT add one. Exactly ONE player exists app-wide, owned by `MediaPlaybackHost` (`src/renderer/components/MediaPlayback/`), mounted once in `App.tsx` and never unmounted: anything rendered per-tab or per-agent is torn down on switch, and removing a media element from the document pauses it. Do NOT mount a second player to play two files at once - the single-player invariant is what makes overlapping audio impossible; `stepMediaItem()` walks the queue in open order, a finished file hands off via `advanceAfterEnded()`, and the history menu jumps by recency. **Queue and history are two lists with opposite lifetimes:** the queue persists (settings key `mediaPlayerQueue`, debounced writes, hydrated in `settingsStore`) while history is per-boot, so history holds whole `MediaItem`s rather than IDs into the queue - never "simplify" it back into pointers, or dropping a queue entry would rewrite what the user already heard. **"Open the player" is one store action, `openPlayer()`**, not a sequence a caller reassembles: the command palette entry and its shortcut both call it, and it resolves `selectMediaPlayerTargetId()` at INVOKE time so a queue that advanced while the palette sat open cannot strand it on a finished file. There is exactly ONE palette entry for it - a second, "Show Floating Media Player", used to appear beside it whenever the widget happened to be minimized, and two commands a word apart that both mean "put the player on screen" is a choice the user has to stop and read for a distinction that is internal bookkeeping. **Both deliberate "show me the player" gestures raise `focusRequest`** (`openPlayer` and `restore`, consumed by a layout effect in `FloatingMediaPlayer`), because the widget is never unmounted and so has no mount to hang focus off: without it a keyboard-opened player is keyboard-DEAD until clicked, and Escape-to-minimize does nothing. Opening or queueing a FILE deliberately does not raise it - that would pull the caret out of whatever the user was typing in. It loads PAUSED at the remembered position - opening the widget is a request to SEE it, not to start listening - and it RE-QUEUES a target that only survives in history, because only queue entries are ever drawn, so activating a history-only id resolved a target, rendered nothing, and read as the feature being broken. That is also why `closeItem` keeps an item's `resumeTimes` entry when the item lands in history: reopening from "recently played" has to resume where the user stopped, and only a track leaving BOTH lists loses its bookmark. Classify media with `getOpenedMediaKind()` in `src/renderer/utils/mediaItems.ts`. **A `file://` link clicked in markdown must go through `openFileUrl()`** (`src/renderer/utils/openFileUrl.ts`), never straight to `shell.openPath`: the file-link plugins emit `file://` for any path outside the project root, so an agent mentioning `~/Scratch/x.mp3` was handing it to the OS player and bypassing the choke point entirely. **The player's height is derived, never stored** (`src/renderer/utils/mediaFloatGeometry.ts`): chrome for audio, chrome + `width / aspect` for video, where the aspect is the file's real `videoWidth / videoHeight` and the chrome is MEASURED (font metrics differ per platform, so a hard-coded transport height letterboxes video somewhere). Only position and a per-kind width persist, so a mixed queue reshapes itself as it advances. **The toast stack steps OVER the widget, never through it:** both open in the bottom-right corner and toasts render at `Z_LAYERS.TOAST` (100000) against the player's 60, so an arriving notification painted straight across the widget and read as the player having closed itself. `FloatingMediaPlayer` publishes its live rect as `floatFootprint` - expressed from the bottom and right edges, so the lane compares the two without needing a viewport measurement of its own - and `ToastContainer` raises itself by `mediaFloatLaneLift()`. Do NOT fix a collision like this by raising the player's z-index instead: toasts have to stay readable over modals, and a video frame would swallow them. **Minimize and close are different actions and must stay that way:** the `-` button sets `dismissed` (widget hidden, audio continues, `NowPlayingIndicator` in the Left Bar header becomes its play/pause via `requestToggle` plus a separate restore button), while `x` calls `closeItem` (player released, sound stops, rest of the queue intact). Minimize is a `hidden` STYLE FLAG on `FloatingMediaPlayer`, never a different render branch in `MediaPlaybackHost` - moving the media element in the React tree unmounts it, and that runs the spec's pause steps. **Escape minimizes, never closes**, and it is scoped to the widget (a `tabIndex={-1}` root plus `onKeyDown`) rather than registered with the layer stack: the player is not modal, it floats over a workspace the user keeps typing in, so Escape is only its to claim while focus is inside it. Wiring Escape to `closeItem` would let a reflex kill playback, which is the one thing the minimize/close split exists to prevent. It defers twice - to an open queue/history list, and to `document.fullscreenElement`, since Escape is already how a user leaves fullscreen - and `MediaViewer` swallows it only when its speed menu is open, so exactly one thing happens per press. **Neither list shows the loaded track:** history records DEPARTURES (a track enters when it stops being active) and `historyForActiveChange` also strips the incoming id, while the queue menu filters via `upcomingMediaItems()` at display time - the active item must stay in `items` or `stepMediaItem` loses prev/next. `clearQueue` keeps the loaded track, since clearing what is queued must not stop playback. **Queueing un-hides the widget** (`enqueueMedia` clears `dismissed` and `dormant`), and that is deliberate rather than a violation of the minimize invariant above: adding files is a request to SEE the queue, so doing it into a hidden player produces no visible response anywhere in the app and reads as the command having done nothing. It is safe because un-hiding is not interrupting - the loaded track is untouched and keeps playing. It fires even when NOTHING was added: queueing files that are all already queued adds zero items, and gating the reveal on `added > 0` made "Add to Play Queue" a silent no-op on exactly the second attempt, which is when a user retries a command that appeared to do nothing. For the same reason the idle branch loads the first file the user POINTED AT rather than the first one newly added, so re-queueing something the queue already holds still puts a widget on screen. A control that hides itself must not silently stop media, and a close button that only hid it leaves audio coming from nowhere. User docs: [media-player.md](docs/media-player.md).
- **Run a shell command from the chat:** `dispatchShellCommand()` (records it in recall history, then runs it) / `runShellCommand()` / `cancelShellCommand()` / `resolveCommandCwd()` in `src/renderer/services/shellCommand.ts`. This is command mode (`!command` in the AI composer). Any new surface that runs a one-off command and shows its output in a transcript should reuse this rather than calling `window.maestro.process.runCommand` directly - the synthetic `{sessionId}-shell-{runId}` id is what keeps the output out of the agent listeners. AI command mode routes its accepted suggestion through the SAME `dispatchShellCommand`, so it runs, records, and recalls identically; do NOT add a parallel run path for it. The one difference is the optional `request` field, stamped onto the card only when the caller passes one - its presence means exactly "this was generated, not typed", and it is what the card shows as provenance and what the next AI command sees as the `Asked:` line. `dispatchShellCommand` forwards its whole options object to `runShellCommand` rather than naming fields, so a new field cannot be silently dropped in between. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#shellcommandts-200-lines).
- **Revealing output the user asked for, past a paused auto-scroll:** `requestTranscriptScrollToBottom(sessionId, tabId)` / `TRANSCRIPT_SCROLL_TO_BOTTOM_EVENT` in `src/renderer/services/transcriptScroll.ts`. The transcript stops following new output once the user scrolls up to read history, which is right for output the AGENT produced on its own schedule and wrong for output the user just asked for by pressing Enter - a bang command's card started streaming offscreen behind the unread badge, so a command that ran looked like it had not. `runShellCommand` raises it ONCE, right after appending the card: from there the transcript's own follow-the-tail handles the stream, and scrolling up mid-run pauses it as usual. It rides one app-level `CustomEvent` (the same shape as `requestHeadingPalette` and `requestFileTreeRefresh`) because the pause lives in `useTerminalOutputScroll`'s local state several levels below the composer. The detail names BOTH the session and the tab and the listener ignores anything else, so a command dispatched into a background conversation cannot yank the view off what the user is reading. Fire it AFTER the content is in the store, or the transcript scrolls to a bottom that does not include it yet, and do NOT use it to force ordinary agent output into view - the pause exists to stop exactly that. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#transcriptscrollts---reveal-output-the-user-asked-for).
- **Delete the file the user is previewing:** `requestFileDeletion({ path, sshRemoteId?, sessionId? })` in `src/renderer/services/fileDeletion.ts`. Opens the shared destructive `confirm` modal, deletes through `window.maestro.fs.delete` (SSH-aware, same IPC the Files panel uses), force-closes every preview tab pointing at the path, and nudges the Files panel through `requestFileTreeRefresh()`. Both the File Preview toolbar's trash button and the command palette's `File: Delete` entry route through it - do NOT hand-roll a second confirm-then-delete path, or the two surfaces drift on what they warn about and what they clean up. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#filedeletionts---delete-the-previewed-file).
- **Telling the Files panel a file appeared or vanished:** `requestFileTreeRefresh(sessionId)` in `src/renderer/utils/fileTreeRefresh.ts` (renderer) and `nudgeFileTreeForPaths(paths)` in `src/cli/services/file-tree-refresh.ts` (CLI). The panel refreshes on a TIMER, so anything that writes or deletes outside that cadence leaves the tree describing a directory that no longer exists, and the user reads that as the save having done nothing. Both ends raise the same `maestro:refreshFileTree` event, whose single app-level listener lives in `useAppRemoteEventListeners` - which is the point: a deeply-nested hook nudges the tree without anyone prop-drilling `refreshFileTree` down to it. Do NOT hand-roll another `new CustomEvent('maestro:refreshFileTree', ...)`; there were four copies with four different comments. Fire it from the function that DID the write (`saveImageToProject` does), not from each caller, so a new surface is covered the moment it renders. The CLI half is deliberately silent and never throws: the bytes are already on disk, so a closed desktop must not turn a good write into a failed command - the loud form is `refreshFileTreeFor()`, behind `maestro-cli refresh-files`, where the refresh IS the command.
- **Ask the model for a shell command (AI command mode):** `requestAiCommand()` / `acceptAiCommand()` / `dismissAiCommand()` in `src/renderer/services/aiCommand.ts`, state in `src/renderer/stores/aiCommandStore.ts`, prompt in `src/prompts/ai-command.md` (id `ai-command`, editable in Settings -> Maestro Prompts). The suggestion runs on the tab's OWN provider at its current model and effort, read-only and with tools disabled. Do NOT hand-roll another one-shot model call for this: the main-process handler (`src/main/ipc/handlers/aiCommand.ts`) goes through the shared `groomContext()` runner.
- **The system-prompt envelope for a provider with no `--append-system-prompt`:** `EMBEDDED_SYSTEM_PROMPT_SEPARATOR`, `embedSystemPromptInPrompt(systemPrompt, userPrompt)`, `stripEmbeddedSystemPrompt(text)`, `hasEmbeddedSystemPrompt(text)` in `src/shared/embeddedSystemPrompt.ts`. Only `claude-code` declares `supportsAppendSystemPrompt`; for every other provider the spawn path has nowhere to put system content except the FIRST user turn, so it sends the whole Maestro system prompt, a `---` rule, and a `# User Request` heading ahead of what the human typed. The provider writes that turn to its session file verbatim, so the envelope is a WIRE FORMAT: changing the separator orphans every transcript already on disk, and a test pins the exact string. Both producers (`src/main/ipc/handlers/process.ts` for the desktop spawn, `src/cli/services/agent-spawner.ts` for the CLI) and the reader (`transcriptMessagesToLogEntries`) go through this module, because a third hand-built copy on the reading side is free to drift from the two on the writing side - which is exactly how a hydrated tab came to render the system prompt, the conductor profile, the cwd and the agent's own id as if the user had typed them (issue #1533). The strip is deliberately conservative: it honours only the FIRST separator (so a user request that quotes the marker survives), it requires something to precede it (a message that OPENS with `# User Request` was never wrapped, and stripping there would delete the user's first line), and only USER turns are unwrapped (an agent explaining the format keeps its answer). Do NOT template the envelope inline at a new spawn site, and do NOT strip it at a call site above `transcriptMessagesToLogEntries` - resume and backfill have to produce byte-identical entries for the same message or the backfill cannot find its splice point. Known gap: the Agent Sessions browser (`SessionMessagesView`) renders the raw messages and still shows the envelope.
- **Consulting another agent from a message (`@mention`):** `planCrossAgentMentions()` / `dispatchCrossAgentMentions()` / `dispatchCrossAgentMentionsForMessage()` in `src/renderer/services/crossAgentMentions.ts`, over `sendCrossAgentRequest()` in `src/renderer/hooks/agent/useCrossAgentDispatch.ts`. Resolution and dispatch are SEPARATE on purpose, because when a consult fires is part of the contract: a message sent while the agent is busy goes to the execution queue, so `useInputProcessing` plans at submit (it needs `suppressLocal` to know whether the source agent answers at all), stamps `crossAgentMention: true` on the `QueuedItem`, and `agentStore.processQueuedItem` fires the consult when the item becomes the agent's turn. **Never consult at submit time** - that pulls the other agent into a question still sitting several messages deep in the queue. This holds for a LEADING mention too (`suppressLocal`, where the source agent does not answer at all): it queues as a `crossAgentOnly` item, and `processQueuedItem` fires the consult and returns without spawning, releasing the dequeue's busy state via `applyQueuedItemRelease`. "This agent doesn't answer it" is not "it has nothing to wait for" - the item's queue POSITION is the user's instruction. Use `hasWorkAheadOfNewMessage()` (`utils/executionQueue.ts`) to make that call. Both entry points are module functions, not hook members, because the queue drain runs outside React. The CLI/web dispatch paths ride the SAME planner: `useRemoteHandlers` plans on the immediate path (a leading mention consults and skips the local spawn) and `useRemoteIntegration` stamps `crossAgentMention`/`crossAgentOnly` on a `dispatch --queue` item - so `maestro-cli dispatch "@Agent ..."` consults exactly like a typed message. Do NOT add a dispatch entry point that hands text straight to a spawn; a mention it never planned is silently inert. Anything that rewrites a queued item's text must re-derive BOTH flags from a fresh plan (`handleEditQueueItem`), never just `crossAgentMention`: where the mention sits is what decides whether this agent answers at all, and an edit can move it, so a stale `crossAgentOnly` silently discards half of what the user changed. And `suppressLocal` with no tab to anchor the reply on is a DROP, not a fallback - letting it through hands the local agent a message addressed to somebody else, which is the one thing the flag exists to prevent. On the main side, `cross-agent-router.ts` spawns the target with `target.customPath || agent.path || agent.command` (same rule as the tab spawn and the Group Chat moderator): detection probes known install dirs BEFORE PATH, so a consult that ignored the agent's `customPath` ran a stale nvm codex stub while the tab ran the real one. **ONLY AGENTS ARE TARGETS.** `resolveMentionedTargetSessionIds()` and `buildKnownMentionNameSet()` (`hooks/input/useAgentMentionCompletion.ts`) both skip groups: a group is picker shorthand that expands to its members' own `@name` tokens at accept time (`memberMentionValue`), so the sent text names agents and nothing else. Resolving a group here too is how an agent that merely SHARED a name with a group had its message fanned out to that whole group - suggestions are built groups-first, so the group won the name every time - and it would also let a message leading with an undispatchable token suppress the local send, addressing it to nobody. **Stop is agent-level and must reach the consults.** A mention fans one turn out across the agent's own tab plus one ephemeral `cross-agent-*` process per target, none of which carry the agent's process id, so `useInterruptHandler` calls `window.maestro.crossAgent.cancel(sessionId)` (non-critical - a failure there must not block the interrupt). Cancellation is addressed by SOURCE AGENT, never by request id: the renderer only learns a request id once `crossAgent.send` resolves, and main holds the authoritative registry (`cancelCrossAgentRequestsForSource`), registered before the target binary is resolved and re-checked around the spawn so a Stop landing in either race still lands. A cancelled consult settles like a timeout but stamped `canceled`, not `error` - the user stopping a consult is not the target failing to answer, and `crossAgentTerminationNote()` is the single place that wording is chosen. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#crossagentmentionsts-115-lines).
- **An agent asking another agent (`maestro-cli ask`):** `runCrossAgentAsk()` in `src/renderer/services/crossAgentAsk.ts`, over the same `sendCrossAgentRequest()` a typed `@mention` uses. `dispatch` is NOT the verb for a question: it writes into the target's ACTIVE tab, so the question lands mid-conversation in whatever the human has open with that agent, and the answer goes to the screen instead of back to the agent that asked. `--background` does not fix that - it decides where the VIEW lands, not which conversation the prompt joins, so a backgrounded dispatch interrupts QUIETLY, which is worse. The path is `cross_agent_ask` (WS) -> `handleCrossAgentAsk` -> the `consultAgent` callback -> `remote:crossAgentAsk` -> `runCrossAgentAsk`, and the ONLY thing it adds over the mention path is `SendCrossAgentRequestOptions.onComplete`, which hands the finished text to a caller blocked on it. Three rules the resolver encodes: the consult is keyed on the CALLING AGENT plus the constant `CROSS_AGENT_ASK_TAB_ID`, never the caller's active tab, so one consult tab per (caller -> target) pairing survives the agent switching tabs and a follow-up `ask` resumes the earlier thread; **no transcript is forwarded by default** (`ask` sends a self-contained question - `--with-context` opts in, and `buildCrossAgentPrompt` swaps to a header that does not announce a transcript that is not there); and it RESOLVES rather than rejects on every outcome including a missing target, because the caller is a CLI process reporting to an agent and an exception there reads as a broken command instead of "that agent could not answer". `onComplete` is guaranteed to fire exactly once, which is why `completedRequests` remembers the COMPLETION rather than just the id: `crossAgent.send` resolves on a round trip to main and "target agent not found" is emitted before it does, so a bare id set let the `.then()` return having never delivered, and the CLI sat there until its own timeout for a consult that failed instantly. The main-side wait is the caller's timeout, not the dispatch path's 3s delivery receipt - here we wait for an ANSWER, not for the renderer to accept a prompt. See [CLI-UI-PARITY.md](docs/agent-guides/CLI-UI-PARITY.md#ask-is-not-dispatch---background).
- **Marking an agent's own hand-off in its transcript (`dispatch` / `ask` from its shell):** `readCallerIdentity()` / `callerMessageFields()` / `readCallerMessageFields()` / `isSelfDispatch()` in `src/shared/agentDelegation.ts`, `noteDispatchDelegation()` in `src/main/web-server/handlers/messageHandlers/agentDelegation.ts`, `recordAgentDelegation()` / `settleAgentDelegation()` in `src/renderer/services/agentDelegation.ts`, drawn by `<AgentDelegationCard>`. Every agent spawn stamps `MAESTRO_CALLER_AGENT_ID` / `MAESTRO_CALLER_TAB_ID`; do NOT reuse `MAESTRO_AGENT_ID`, which tells `pianola watch` it is Pianola.
- **Sending a prompt to an agent from outside the composer:** `enqueuePromptForTab()` / `buildQueuedMessageItem()` in `src/renderer/services/queuedPrompt.ts`. Anything that asks an agent a question without a user typing it (the CLI's `dispatch --queue`, a snooze's wake prompt) has the same two problems: the item must be byte-identical to one the composer would have built, and it must not race whatever the agent is already doing. **Queue it, do not spawn it.** The queue answers both - `useQueueProcessing` drains an idle agent on its next render and a busy one when its turn ends, so no caller re-implements "is the agent free?", and the target tab is re-resolved at DRAIN time (`resolveQueuedItemTarget`), which is what makes this safe to call in the same tick as the store write that created the tab. Firing `maestro:remoteCommand` there instead reads a render-time `sessionsRef` and silently drops the prompt. Three fields are easy to omit and each has a visible cost: `tabName` (what a closed tab's item falls back to), `readOnlyMode` (what lets it bypass the parallel-execution guard), and `turnSettings` via `captureQueuedTurnSettings` (what keeps a queued turn on the model it was queued with). The `@mention` flags are STAMPED here and fired by `processQueuedItem` - see the entry above. Module functions, not a hook, because the callers are a sweep timer and an IPC listener. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#queuedpromptts---send-a-prompt-without-a-composer).
- **What a snooze opener was handed:** `resolveSnoozeTarget(session, id)` in `src/renderer/utils/snoozeHelpers.ts`, returning `{ tabId, tabLabel, canRunWakePrompt }`. Every entry point gets ONE id and no kind - the tab strip passes whatever chip was right-clicked (any of the four kinds, or a tiled GROUP), while the shortcut and the palette pass the active AI tab - so the kind is resolved here rather than at each opener. It checks `tabGroups` first, then the unified tab order, which is the same lookup `snoozeTab` itself does, so an id this resolves is an id that will park. `MainPanel.handleOpenSnooze` searched `aiTabs` alone and returned early for everything else, which made "Snooze Tab" on file/terminal/browser chips and "Snooze group" on a group chip silently do nothing. `canRunWakePrompt` must be DERIVED, never hard-coded beside it: only a conversation can be prompted on return (`canSnoozeRunWakePrompt`), so a file, terminal, or browser tab hides the field rather than collecting a prompt that could never be sent, and a group qualifies on holding one AI pane. Both ends of a snooze's transcript mirror run `collectSnoozedAiTabs(entry)` - a group holds one transcript per AI pane, and a copy taken on the way in that nothing releases on the way out is a mirror kept forever.
- **Parking, waking, dismissing, or rescheduling ONE snooze:** `snoozeTabWithMirror()`, `wakeSnoozeNow()`, `dismissSnoozeNow()`, `rescheduleSnoozeNow()` in `src/renderer/services/snoozeActions.ts`, with `runRemoteSnoozeCommand()` beside them for the CLI's far end. None of the four verbs is just the store write, and the steps they drag behind them fail INVISIBLY: the transcript mirror must be taken BEFORE the snooze (the write removes the tabs and their `agentSessionId`s from the session, so a mirror taken afterwards has nothing left to resolve a transcript from - and a snooze easily outruns the provider's retention, which is why the mirror exists at all), and a wake or dismiss must RELEASE that mirror and RECORD the resolution (releasing rehydrates a transcript the provider aged out while the tab was parked; the record is the note the user left themselves). Three callers share it - the Snooze dialog, the Snoozed Tabs list, and `maestro-cli snooze` - and the sequence was written out at each of the first two before this module. `sessionId` is optional and defaults to the ACTIVE agent, which is what every click path means; a scripted snooze names its own agent, since "active" is whatever the human happens to be looking at. `announce` suppresses the NOTICE, never the work (`--background` lands here): the parked tab leaving the strip IS the verb rather than placement, so what the flag buys is not flashing "Snoozed until ..." at someone looking at a different agent. The wire shape is `src/shared/snoozeCommands.ts` - ONE `snooze_command` message with an `action`, not six messages, because the six verbs differ only in which id they name. Do NOT hand-roll another mirror-then-write sequence at a new call site. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#snoozeactionsts---the-four-single-snooze-operations-with-their-side-effects).
- **Appending to a transcript entry:** `canAppendToLogEntry(entry, source)` / `isSelfContainedCard(entry)` in `src/renderer/utils/logEntries.ts`. A `LogEntry` is either an open stream (coalesce consecutive chunks into it) or a self-contained card that owns its own text (`shellCommand`, `retryOutageId`, `recoveryAction`, `aiCommand`, `metadata.hiddenProgress`, `metadata.toolState`). Cards keep a NATURAL source - a `!` command's card is `source: 'stdout'` because its body really is terminal output - so **never gate coalescing on `source` alone**; that's how agent replies ended up rendered inside a command's output box. Adding a new card kind means adding its marker to `isSelfContainedCard`, which fixes every coalescing site at once.
- **Command mode (`!`) is STATE, not a text prefix, and it is a LADDER:** the live value is `composerInputStore.aiCommandMode` (`'off' | 'shell' | 'ai'`), persisted per tab as `AITab.commandMode`. `!` on an EMPTY composer climbs one rung (agent -> `'shell'`, a literal command line -> `'ai'`, a plain-English request the model turns into one); Escape on an empty composer climbs back down; the caret never leaves the textarea. The bang is consumed on entry and never appears in the draft, so NEVER test the text for a leading `!` to detect the mode - a real command can contain bangs, and the draft won't start with one. **Never test the value for truthiness either: `'off'` is a truthy string.** Compare against the literals, or use `isShellCommandMode()` / `isAiCommandMode()`. Persisted values from older builds are booleans (`true` meant `'shell'`), so always read `AITab.commandMode` through `normalizeComposerCommandMode()`. The invariant: the same string is a shell command, a request for one, or an agent message depending only on this value, so any path that persists or restores `inputValue` MUST carry `commandMode` with it (`syncAiInputToSession` reads it from the store itself so a caller can't forget). Helpers all live in `src/renderer/utils/shellCommandInput.ts`. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#command-mode-is-state-not-a-text-prefix).
- **Shell tab completion:** `useTabCompletion()` in `src/renderer/hooks/input/useTabCompletion.ts` serves BOTH the terminal composer and command mode, told apart by its third `commandMode` argument (the text can't say which - a command line looks the same either way). Terminal mode completes against `shellCwd` + `shellCommandHistory`; command mode completes against the project root + the bang-prefixed entries in `aiCommandHistory`, because a command-mode command runs at the agent's `cwd` regardless of where a terminal tab has `cd`-ed. Suggestion values are plain command lines in both cases. Add completion sources here, not in a parallel hook.
- **Right-click menu on an image:** already done, app-wide. `<ImageContextMenuHost>` (`src/renderer/components/ImageContextMenuHost.tsx`) is mounted once in `App.tsx` and runs ONE delegated `contextmenu` listener that resolves the image from the click target, so raster `<img>`, inline `<svg>`, Mermaid charts, thumbnails, and the lightbox all get Copy Image / Save to Project / Save As with **zero per-surface wiring**. Do NOT add an `onContextMenu` to a new image surface or hand-roll copy/save buttons - a new surface is covered the moment it renders. To opt a surface out (it owns its own right-click behavior), put `data-no-image-menu` on its container. Export helpers live in `src/renderer/utils/imageExport.ts` (`saveImageToProject()` writes to `.maestro/diagrams/` and works over SSH). See [UI-PATTERNS.md → Right-Click Image Menu](docs/agent-guides/UI-PATTERNS.md#right-click-image-menu-imagecontextmenuhost).
- **Font zoom on a reading pane:** `useFontScale(storageKey)` in `src/renderer/hooks/ui/useFontScale.ts` + `<FontScaleControl>` in `src/renderer/components/ui/FontScaleControl.tsx` (`variant="inline"` for a toolbar, `variant="floating"` for a pill over a scrolling pane). The hook owns clamping, two-decimal rounding, and localStorage; do NOT hand-roll another `AArrowUp`/`AArrowDown` pair or another `clampFontScale`. Render it ONLY where the zoom moves type - Director's Notes hides it in Rich Mode, and the file preview gates it on `canScaleFontForView()` in `filePreviewUtils.ts`. See [UI-PATTERNS.md → `<FontScaleControl>`](docs/agent-guides/UI-PATTERNS.md#fontscalecontrol-srcrenderercomponentsuifontscalecontroltsx).
- **Bare `+` / `-` / `0` zoom on a surface:** `useScaleShortcuts(control, { enabled })` in `src/renderer/hooks/ui/useScaleShortcuts.ts`, over any `useScalePreference` control, with the keys named in the buttons' tooltips via `<ScaleControl shortcutHint>`. It is deliberately MODIFIER-FREE and skips any event carrying Cmd/Ctrl/Alt, because `Cmd+=` / `Cmd+-` is the app-wide font zoom and must keep working while a zoomable surface is open. It listens on `window` at CAPTURE rather than on the surface's node: focus falls to the body whenever a nested overlay closes, so a node-scoped handler goes dead exactly when the user comes back to the surface, and `stopPropagation` is what keeps a bare `0` or `-` out of the global shortcut handler. **Gate it with `enabled`, normally `useIsTopLayer(priority)`** - two stacked surfaces both answering the same unmodified key is the failure this replaces. `FilePreview` keeps its own inline copy of the same branch because that one sits inside a single ordered key chain where position decides which branch wins; every other surface rides the hook.
- **Whether a surface is the topmost layer:** `useIsTopLayer(priority)` in `src/renderer/hooks/ui/useIsTopLayer.ts`. Matching is by `MODAL_PRIORITIES` value rather than layer id, because `useModalLayer` owns the registration and never hands the id back. Reach for it whenever a surface binds an unmodified key or wants to reclaim focus (`AutoRunExpandedModal`) - do NOT re-derive `getLayers()[length - 1]` inline.
- **Who asked for this turn (interactive vs automation):** `QUERY_SOURCE_ENV_VAR` / `QuerySource` / `DEFAULT_QUERY_SOURCE` in `src/shared/querySource.ts`. A turn you typed, an Auto Run task and a Cue subscription all produce the SAME process - same binary, same args, same cwd, same transcript - so anything hanging off the agent (Claude Code hooks, wrapper scripts, telemetry sidecars) had only the prompt text to go on, which is a guess: Cue prompts are the user's own words out of `cue.yaml`. Every spawn now stamps `MAESTRO_QUERY_SOURCE` into the child env. The origin is CLAIMED at the dispatch site, never inferred downstream, and ties break toward `'user'` so delegation is never over-reported. It is stamped AFTER the user-editable env layers, or a stray global of the same name would relabel every turn on the machine. SSH re-states it explicitly in `wrap-spawn-for-ssh.ts` (only `customEnvVars` crosses to the remote host, so the local process env never arrives). Terminal PTYs are deliberately unmarked - a shell is not an agent turn.
- **Who SENT this turn (Web Login attribution):** `getActingUser()` in `src/main/web-server/auth/acting-user.ts`, `noteTurnActor()` / `resolveTurnActor()` / `forgetAgentActors()` in `src/main/web-server/auth/turn-attribution.ts`. The account is in scope at SPAWN and nowhere else: the one-shot effects of a turn - the `history:add` entry and the `stats:record-query` row - are written LATER by the DESKTOP renderer's exit listener, which owns them for every client, and by then `getActingUser()` answers `undefined` no matter who sent the turn. So do NOT read `getActingUser()` at write time and expect a browser account - a turn sent from a phone would be recorded as if it had been typed at the keyboard. `process:spawn` notes the actor keyed by agent + tab and the two write handlers look it back up, stamping `HistoryEntry.userName` / `userDisplayName` (the History row's sender pill and its `UserSourceFilter`) and `query_events.user_name`. One entry per tab, overwritten by the next spawn, because a tab runs one turn at a time. A DESKTOP spawn passes `undefined`, which CLEARS the entry - deliberately, so a phone's earlier turn is never credited to a later one typed at the keyboard - and closing an agent drops everything it owned via `forgetAgentActors`. The same spawn stamps `QUERY_USER_ENV_VAR` (`MAESTRO_QUERY_USER`) into the agent's environment at the same injection point as the caller-identity vars, for the same reason: it has to reach both the local and the SSH env merges. Terminal tabs are excluded from both - a shell the user drives is not an agent turn.
- **An agent's effective environment:** `resolveAgentEnvironment()`, `isSecretEnvKey()`, `maskEnvValue()`, `envSourceLabel()` in `src/shared/agentEnvironment.ts`, rendered by `<EnvVarList>` in `src/renderer/components/ui/EnvVarList.tsx`. Three layers decide what an agent runs as (global Settings -> Environment, then per-provider Settings -> Agents, then the agent's own overrides), and each is edited in a different pane, so no single settings screen answers "which profile is this actually running as?". The module does the SAME merge as `process:spawnTerminalTab` and reports which layer won plus which it shadowed. Do NOT re-derive the precedence inline: a merge that disagrees with the spawner's describes a process nobody is running. Distinct from `Settings/EnvVarsEditor`, which EDITS one layer - pick by question ("change a value" vs "which profile am I on?") and do not add an edit mode to `<EnvVarList>` to cover both. **A variable switched off with the editor's eye button lives in a SECOND record** (`shellEnvVarsDisabled`, same shape as `shellEnvVars`), and nothing but the editor may read it: parking is what keeps the value recoverable without it reaching a process, so being present in `shellEnvVars` is exactly the same statement as being live, and every consumer stays filter-free. Do NOT merge the disabled record into a spawn env or teach a spawner to skip entries - a single `enabled` flag inside one record would put that filter in every one of the dozen-odd read sites, and the first one that forgets it silently ships the API key the user thought they had switched off. Secret-looking keys are masked behind a per-row reveal, matched loosely on purpose: these panels get opened while screen-sharing for help, so a false positive costs one click and a false negative leaks a live key.
- **Tiny uppercase state chip:** `<MiniBadge>` in `src/renderer/components/ui/MiniBadge.tsx` - "WT" beside a worktree agent, "Snoozed" beside a tab. The generic text counterpart to `<CountBadge>` (a number and nothing else) and to the fixed-word domain pills beside it (`WorktreePill`, `GitRunningBadge`). Do NOT hand-roll another `text-[9px] px-1 rounded uppercase` span: the same chip was written per surface and drifts on padding and weight. The label is its own accessible name, so pass a real word rather than an abbreviation, unless the abbreviation is the established UI term (then pass `title` with the long form).
- **The red Stop pill:** `<StopTurnButton>` in `src/renderer/components/ui/StopTurnButton.tsx`. Stop is ONE agent-level action - it signals the agent's own process, every other busy tab it owns, and every cross-agent consult it fanned out - so every surface offering it draws the same control. Two surfaces do: the thinking pill (while the agent is working) and the cross-agent "N agents responding" pill (when only consulted agents are working, so the thinking pill is absent). They are mutually exclusive by construction in `InputArea` - the indicator is handed `onInterrupt` only when the thinking pill's own render condition is false - because both buttons run the same interrupt and two on screen is one control drawn twice. Do NOT hand-roll another red pill: a copy drifts on the square glyph, the padding, and worse, on what its tooltip claims the button stops.
- **"Show only providers this machine can run":** `filterToAvailableProviders()` / `providerLocationLabel()` in `src/renderer/utils/providerAvailability.ts`, rendered by `<ProviderAvailabilityBar>` in `src/renderer/components/ui/ProviderAvailabilityBar.tsx`. Both provider pickers (the wizard's tile strip, the New Agent modal's list) offer one toggle between "everything supported" and "everything this machine can run", and the rules live in `shared` code so the two cannot drift on what "available" means. **Which way the toggle STARTS is per-picker and deliberately differs:** the New Agent modal opens filtered (the everyday path, whose user knows what they installed), the wizard opens on the full list (a first-run screen, where someone whose provider is installed but undetected would otherwise conclude Maestro does not support it). Three invariants: the count describes ALL supported providers, never the filtered rows (a count that shrank with the list reads "4 of 4" and answers nothing); filtering down to nothing falls back to the FULL list (an empty picker has no row to reach per-provider settings through, so a binary in a non-standard place becomes unreachable); and the SELECTED provider survives the filter (duplicating an agent whose provider is missing would otherwise hide the row showing what is selected). Say `providerLocationLabel(remoteHost)` rather than hard-coding "locally" - both pickers can point at an SSH remote, and the counts describe whichever host was probed. Flipping the filter renumbers the list, so carry the focus ring by PROVIDER, not by index. It also changes the tile COUNT, and in the wizard that changes the layout: `resolveAgentGridLayout()` (`Wizard/screens/AgentSelectionScreen/utils/agentGridLayout.ts`) draws a centered wrapping block whenever the tiles fit in two rows and only falls back to the scrolling strip past that, with rows BALANCED (five tiles are 3 + 2, never 4 + 1) and the column count reported upward so arrow-key movement steps by the layout actually drawn. See [UI-PATTERNS.md → Two Shapes for One Tile Set](docs/agent-guides/UI-PATTERNS.md#two-shapes-for-one-tile-set-agentgridlayout).
- **Whether a login flow can fix an auth failure:** `classifyCredentialKind()` and `credentialKindBlocksLogin()` in `src/shared/providerAuthIdentity.ts`, which also owns `providerAuthKey()` (the credential's shared scope: agent binary + host). NOT every credential is an OAuth login. An agent authenticating with `ANTHROPIC_API_KEY`, one pointed at a gateway via `ANTHROPIC_BASE_URL`, and one running on Bedrock/Vertex all fail with the SAME `auth_expired` output an expired login produces, and running the provider's login command for them produces a successful-looking flow that changes nothing the agent presents - the user believes it is fixed and the next prompt burns on the same rejection. So any surface that offers to sign the user in gates on this first (`ReauthModal` does), and reports the real remedy instead. Feed it the effective env from `resolveAgentEnvironment()`, never a partial one: a classification built from a different merge describes a process nobody is running. Related: an error `message` in `src/shared/agentErrorPatterns.ts` must name WHAT failed and never a terminal command, because the bank does not know which of these kinds produced the line.
- **Typing a login command into a shell:** `formatAgentLoginCommand(login, syntax?)` and `loginShellSyntaxFor(shellId, isWindows)` in `src/shared/agentMetadata.ts`. Rendering the line is not string concatenation, because the shell it lands in changes what runs. PowerShell parses a line that STARTS with a quoted string as an expression and echoes it, so `"C:\Program Files\Claude\claude.exe" /login` prints a path and logs nobody in; the call operator `&` is what makes it a command, and only PowerShell wants it - cmd.exe runs the quoted path directly. Windows agents installing under `C:\Program Files\...` makes this the common case there, not an edge case. So ask `loginShellSyntaxFor` for the dialect rather than sniffing the shell id at the call site: off Windows everything is `posix`, and ON Windows Git Bash and WSL are still `posix` while an unset or unknown shell falls back to `powershell`. Two related rules live with the caller (`ReauthModal`): the line is submitted with **CR, not LF** (ConPTY delivers LF as Ctrl+J, which PSReadLine ignores), and a Windows `wsl` default shell is replaced with `powershell`, because agents spawn as native processes and a login inside WSL writes credentials the agent never reads. An SSH remote is exempt from both - its shell is the remote host's.
- **Bucketing Director's Notes bullets:** `bucketNarrativeItems()` / `shouldRenderBuckets()` / `buildNarrativeGroupLookup()` in `src/shared/directorNotesGrouping.ts`, bound to live store state by `useNarrativeGroupLookup()` in `src/renderer/components/DirectorNotes/`. The synopsis agent emits a flat list of bullets per section, each tagged with the session it came from; the GROUP that session belongs to is Maestro's answer, never the model's, which is why it is derived from `sessionStore` instead of being one more field the model can invent. Two surfaces render the same narrative - Rich Mode (`NarrativeSections`) and the markdown path behind Plain Mode / Copy / Save (`narrativeToMarkdown`) - so the bucketing rule lives in one pure module they share; a second copy drifts on the parts that are easy to get subtly wrong: an agent name is matched through `normalizeAgentKey` (the manifest hands the model a sanitized display name, so it rarely matches a stored name byte for byte), a blank `agent` string means the same thing as no agent, the unattributed catch-all always sinks last, the agent pill survives only inside a GROUP bucket (under an agent header it just repeats the header), and a section with ONE bucket stays a flat list because a lone subheading only restates the section title. Do NOT add a `group` field to the prompt contract in `src/prompts/director-notes.md` - the model's job ends at naming the session.
- **Which provider generates a synopsis:** `AUTO_SYNOPSIS_PROVIDER`, `synopsisProviderChoice()`, `pickFirstAvailableProvider()` in `src/shared/directorNotesProvider.ts`; `resolveSynopsisProvider()` in `src/main/utils/director-notes-provider.ts`
- **Sortable table header:** `<SortableTh>` in `src/renderer/components/ui/SortableTh.tsx` + `useTableSort()` in `src/renderer/hooks/ui/useTableSort.ts`. The hook owns the flip-vs-switch rule (clicking the ACTIVE column reverses it; clicking a DIFFERENT column jumps to that column's own default direction instead of inheriting the previous one - inheriting is what makes a freshly-picked column show Z-A or least-used-first and read as broken data). The component owns `aria-sort` placement (on the `<th>`, not the control), a stable caret slot so switching columns doesn't reflow the header row, and a real `<button>` for the click target. Do NOT hand-roll `<th role="button" onClick>`: `role` grants the semantics without the tab stop or Enter/Space handling, so it announces as a button and then does nothing from the keyboard.
- **Graphing a set of documents rather than one:** `BuildOptions.scopeFiles` / `scopeDirectory` in `src/renderer/components/DocumentGraph/graphDataBuilder.ts`, opened via `openGraphScope()` in `fileExplorerStore.ts`. Focus mode ("what does this document reach?") only creates nodes for files its BFS walks INTO, so a file nothing links to can never appear; scope mode ("how do these documents relate?") makes a node for every file in the set. Unreachable documents were being dropped TWICE - the builder's BFS, and then again by `prepareLayoutInput()` in `mindMapLayouts.ts`, which all six layout algorithms funnel through - so fixing only the builder renders nothing. They ride a `showOrphans` flag into the layout and get their own band via `positionOrphanNodesBottom()`, NOT a borrowed depth ring: an unreachable node has no depth, and giving it one draws it as a child of a center it has no link to. Scope mode also skips `startBacklinkScan`, which would pull in files outside the scope - the exact files the user did not select.
- **A labeled action button in a panel header:** `<HeaderActionButton>` in `src/renderer/components/ui/HeaderActionButton.tsx` - the "+ New Memory" / "+ New Session" / "Resume" pill. It was hand-rolled in five files as the same `px-3 py-1.5 rounded-lg text-sm font-medium` string. It sizes ONE STEP BELOW the header title (`text-xs`, icon `w-3.5`), deliberately: the copies used `text-sm`, which is exactly the title's size, so the button read as heavy as the heading it sits beside and dominated a header it is subordinate to. Pass the icon without size classes - the component sizes it. Do NOT hand-roll another accent pill, and do not "fix" it back to `text-sm`.
- **Highlighting a search hit:** `highlightMatches(text, query, accentColor)` in `src/renderer/utils/highlightMatches.tsx` for ordinary text, and `<TextareaHighlightOverlay>` in `src/renderer/components/ui/TextareaHighlightOverlay.tsx` for text inside an editable `<textarea>`. Both split on the shared `splitOnMatches()`, so the regex escaping and the match parity cannot drift between them. A textarea renders plain text and nothing else, so the overlay is a backdrop UNDER it whose own glyphs are transparent - the visible characters are always the real editable ones and only the mark backgrounds show through, which keeps the caret, selection, undo stack, and IME intact. Its font, padding, and border width are COPIED FROM THE LIVE COMPUTED STYLE rather than restated in CSS: a duplicated font rule works right up until someone edits the textarea's and not the overlay's, and every highlight then lands a few pixels off the word it belongs to. Do NOT swap the textarea for a contenteditable to get markup - that trades a one-off alignment problem for a permanent input-handling one.
- **Highlighting a FUZZY hit:** `renderFuzzyHighlight(text, indices, styles?)`, `fuzzyMatchWithIndices(text, query)` in `src/renderer/utils/search.ts`. Distinct from `highlightMatches` above, which marks SUBSTRING hits: a fuzzy matcher scores non-contiguous characters, so it has to be handed the matched INDICES rather than re-deriving spans from the query.
- **Opening the expanded staged-image organizer:** `requestOpenStagedImagesOrganizer()` and `OPEN_STAGED_IMAGES_ORGANIZER_EVENT` in `src/renderer/services/stagedImagesOrganizer.ts`. The organizer's open state is LOCAL to `StagedImagesStrip`, which sits several levels below the app-level keyboard handler that owns the `openImageOrganizer` shortcut, so the request rides one app-level `CustomEvent` in the same shape as `requestHeadingPalette` and `requestTranscriptScrollToBottom` rather than a callback drilled up through `InputArea` and `MainPanel`. It is fire-and-forget on purpose: the strip only mounts while images are staged, so a keypress with an empty composer is a silent no-op instead of an empty modal. Both ends check `stagedImages.length > 1` - the handler so the shortcut is not recorded as a tracked event when there is nothing to organize, the strip because it is the one that knows the live count. One image is the lightbox's job (`Cmd+Y`), not the organizer's. Do NOT hand-roll another dispatch of this event name, and do NOT prop-drill the setter instead: exactly one strip is mounted at a time, which is what makes the event unambiguous.
- **A list of editable `key = value` pairs:** `<KeyValueRows>` in `src/renderer/components/ui/KeyValueRows.tsx`, with `recordToKeyValueRows()` / `keyValueRowsToRecords()` beside it. The plain form of the control: a row per pair, a monospace input on each side, an optional eye that parks the row, a trash button. An SSH remote's environment variables and its extra `ssh -o` options are the same widget with different labels, and the second was about to become a copy of the first. Rows carry a stable `id` rather than being keyed by their key text - a list keyed by the editable field remounts the input on every keystroke and the caret jumps out after one character. `keyValueRowsToRecords` returns BOTH halves together (each `undefined` rather than `{}` when nothing survives, so a section the user emptied stores no key at all): a caller that wrote only the half it remembered would silently drop the other, which for the parked half means losing exactly the values parking exists to keep. The eye is opt-in via `onToggleRow` - without somewhere to keep a parked value there is nothing the button could do, so it is hidden rather than rendered dead. Only LIVE rows are validated by the consumer, because a value that cannot work right now is precisely what gets parked and a parked row that blocked Save would defeat the control. Distinct from `Settings/EnvVarsEditor`, which is the SHELL environment editor and owns secret masking, known-auth path completion, and absolute-path validation - pick by whether those behaviours are wanted, and do not add a mode to either to cover the other.
- **Switching a `key = value` entry off without deleting it:** `splitParkedEntries()` / `setKeysParked()` / `parkedPairKeys()` in `src/shared/parkedRecords.ts`. TWO records, never one record with an `enabled` flag. Being present in the ACTIVE record is exactly the same statement as being live, so every consumer - `resolveSshOptions`, the four SSH spawn sites, `resolveAgentEnvironment` - keeps reading one record and needs no filter; a single flag inside one record would put that filter in every read site, and the first one that forgets it ships the `ProxyCommand` the user thought they had switched off. The rule the module enforces so no caller has to: a key lives in exactly ONE of the two records, and a live row wins the key outright, because a key in both is unreachable state (re-enabling the parked copy after deleting the live one silently restores a value the user cannot see). `setKeysParked` IGNORES a key present in neither record rather than inventing it, so disabling something already deleted is a no-op instead of a resurrection. Three things ride it: the eye in `<KeyValueRows>`, the save handler, and the CLI's `--disable-*` / `--enable-*` flags, where disable runs BEFORE enable so one command can swap which of two keys is live. A `--clear-*` verb must wipe BOTH records - "remove all" that left parked entries behind leaves invisible state a later `--enable-*` could resurrect. Field pairs today: `shellEnvVars`/`shellEnvVarsDisabled`, `SshRemoteConfig.sshOptions`/`sshOptionsDisabled`, `SshRemoteConfig.remoteEnv`/`remoteEnvDisabled`.
- **A text box that narrows a list:** `<FilterInput>` in `src/renderer/components/ui/FilterInput.tsx` - search icon, borderless input, optional `resultLabel` count, and a clear button that appears only once there is something to clear. Do NOT hand-roll another `<Search/> + <input>` pill; there are already a dozen near-identical copies that disagree on whether a clear button exists and what Escape does. Distinct from a FIND bar (`AutoRunSearchBar`, `TerminalSearchBar`), which walks matches inside one document and owns next/prev plus a match index - pick by question ("which rows do I see?" vs "take me to the next hit") and do not add match navigation to `<FilterInput>`. **Escape needs the host's help inside a modal:** the layer stack handles the key at capture on `window`, so the input never sees it and the surface closes instead of the filter clearing. Clear the query from the host's own `onEscape` first (see `MemoryViewer`). **Do NOT add a collapsed/magnifier-only mode to it** - that was tried and removed: hiding a pane's primary control to buy space in a crowded toolbar trades a layout problem for a discoverability one. When a toolbar cannot fit on one line, move what is NOT a control off that line (the Memory Viewer's corpus stats went to a footer).
- **A pane that reads and edits a markdown document:** the File Preview stack, never a bare `<textarea>`. `<Markdown preset="document">` for reading (scoped with `generateProseStyles({ theme, scopeSelector })` so its heading and table rules do not leak onto the surrounding chrome), `<MarkdownEditor>` from `src/renderer/components/FilePreview/markdownEditor` for writing (CodeMirror colouring, wrap-aware line-number gutter, imperative `focus` / `scrollToLine` / `setSearchMatches` handle), and `Cmd/Ctrl+E` to flip between them - read from the user's LIVE `toggleMarkdownMode` binding via `eventMatchesShortcutKeys`, never from a literal `e`, or the chord that flips a file preview and the chord that flips a memory drift apart after a remap. **Open in Preview**: these panes are opened to read far more often than to write. A pane registered through `useModalLayer` must bind the chord ITSELF (it blocks the app-level handler), must hand the caret to the editor on the way in and back to the list on the way out, and must key the editor on the filename - undo history belongs to one document, and carried across a switch an undo pastes the previous file's text into this one. Search highlights are PUSHED through `setSearchMatches()` from an effect rather than passed as a prop (CM6 owns its document; rebuilding the view discards the undo stack and the caret), built from the same `splitOnMatches()` the preview highlights with so the two modes cannot disagree about what is a hit. `MemoryViewer` is the reference implementation. See [UI-PATTERNS.md → A Surface That Reads and Edits Markdown](docs/agent-guides/UI-PATTERNS.md#a-surface-that-reads-and-edits-markdown).
- **Jumping a markdown preview to a heading:** `scrollToHeadingSlug()`, `headingLevelColor()` in `src/renderer/components/FilePreview/shared/headings.ts`, reached from a modal via `requestHeadingPalette()` / `HEADING_PALETTE_EVENT` in `src/renderer/services/headingPalette.ts`. One jump path serves the ToC panel and the `#` heading palette, so the two cannot drift on how a jump works per preview tier - the virtualized Fast tier keeps most headings out of the DOM, so a plain `querySelector` finds nothing. The `#` keycap advertising the palette is opt-in on the shared `TocOverlay` (`searchShortcutKey`), because the palette belongs to the SURFACE: File Preview binds `#`, Director's Notes does not, and advertising a key nothing binds sends the user to press a dead key.
- **Which heading the reader is under right now:** `findActiveHeadingSlug(scroller, container, onReadActive?)` plus `ACTIVE_HEADING_FOLD_PX` in `src/renderer/components/FilePreview/shared/headings.ts`. This is the read half of the jump above, and it has the SAME tier problem: Rich and Giant mount every heading so a DOM walk answers it, while the Fast tier virtualizes its blocks and its headings mostly do not exist in the DOM. The `onReadActive` override is how the Fast tier answers from its parsed block array (`getActiveHeadingSlug` on `MarkdownPreviewFastHandle`), and it is tri-state on purpose: a slug means "this one", `null` means "above the first heading" (a real answer, not a miss), and `undefined` means "no opinion" and falls through to the DOM walk, so callers never have to know which tier is mounted. The DOM walk BINARY SEARCHES: headings sit in document order so their tops increase monotonically, and this runs on every scroll frame - do not turn it back into a linear `find`. `FilePreview` owns the measurement because only it holds both the scroller and the container ref, throttles it into the existing scroll listener, and skips the work entirely while the overlay is closed. Do NOT let `FilePreviewToc` measure for itself.
- **Highlight vs selection in a heading list:** `FilePreviewToc` takes `activeIndex` (where the document is scrolled) and keeps its own `selectedIndex` (which row is lit). They are separate for two reasons: a click or arrow press must light the row IMMEDIATELY rather than waiting for the jump's scroll to land, and near the bottom of a document the scroll clamps so `activeIndex` stops changing - a list driven only by the document would freeze and the last few headings would be unreachable by keyboard. `-1` is a real value on both: it means the reader is above the first heading, so no row is lit and the `Top` sash gets the accent rail instead.
- **Keyboard navigation in a `<DualPaneFileEditor>` list:** built in. Up/Down walk the VISIBLE rows (collapsed categories are skipped, the ends do not wrap), and Backspace/Delete raise `onDeleteItem(selectedId)` - the list reports the intent, the consumer owns the confirmation. Both keys are ignored unless the event came from a row, so the "+ New" button in the same container cannot delete anything. Pass `autoFocusList` when the surface's whole job is walking the list (the Memory Viewer) so the keys work without clicking a row first; it fires on the FIRST selection rather than on mount (the list loads async) and defers to anything that already holds focus, so a user who went straight for the filter box keeps the caret. After a consumer-driven delete, bump `listFocusToken`: the focused row was just unmounted, focus falls to `<body>`, and the next Backspace silently does nothing, which reads as the keyboard dying halfway through a cleanup pass.
- **Segmented toolbar (sort/filter pill bar):** `<SegmentedControl>` in `src/renderer/components/ui/SegmentedControl.tsx`. The "Sort by: [Name][Created][Queries]" bar above a grid or chart. It owns active-segment coloring, seam borders, `role="radiogroup"` semantics, arrow-key navigation, and a single tab stop; each segment gets `data-testid="${testId}-${value}"`. Do NOT hand-roll another `.map()` over buttons with `borderLeft` seams. Distinct from `<RadioGroup>`, which is the stacked list-row form for settings panes - pick by layout, and do not add a `variant` prop to either to cover the other. See [UI-PATTERNS.md → Segmented Toolbars](docs/agent-guides/UI-PATTERNS.md#segmented-toolbars-segmentedcontrol).
- **Paginating a list already in memory:** `usePagination(items, pageSize, resetKey)` in `src/renderer/hooks/ui/usePagination.ts`, pure helpers in `src/renderer/utils/pagination.ts`, UI in `<Pager>` (`src/renderer/components/ui/Pager.tsx`). Do NOT confuse it with `useHistoryPagination`, which is the async IPC-backed windowing engine for data that arrives page by page - this one is for a list you already hold. The hook clamps the current page on every read rather than in an effect, which is what stops a filter that shrinks 1236 rows to 18 from rendering an empty grid under "Page 30 of 1"; pass a `resetKey` built from whatever the user can change that reorders the list (sort mode, filter mode, query text) so re-sorting does not strand them on an arbitrary slice. Render `<Pager>` in the TOOLBAR ROW, not under the list: a pager below a long grid inside a scrolling modal makes the user scroll to the bottom, click, then scroll back up.
- **Following streaming output in a capped box:** `useStickToBottom(contentKey)` in `src/renderer/hooks/ui/useStickToBottom.ts` - returns a callback ref that keeps a scrolling box pinned to its newest content and releases the moment the user scrolls up. Reach for it whenever a box has BOTH a capped height and content that arrives over time: once it hits its cap it stops growing, so the transcript's own auto-scroll has nothing left to follow and the user is stranded on the FIRST screen of output while the live tail piles up below. Pinning is recomputed from geometry on every scroll event, never remembered as a flag - a flag has to tell a programmatic scroll from a user one, and scrolling to where you already are fires no event to clear the guard. Do NOT collapse it into a boolean. Distinct from `useScrollIntoView` (one element in a list) and `TerminalOutput`'s MutationObserver (the whole pane). See [UI-PATTERNS.md → Following Streaming Output](docs/agent-guides/UI-PATTERNS.md#following-streaming-output-usesticktobottom).
- **Restoring an AI tab's scroll position:** the `initialScrollTop` + `initialIsAtBottom` pair, consumed by `useTerminalOutputScroll()` in `src/renderer/components/TerminalOutput/hooks/useTerminalOutputScroll.ts`. `initialScrollTop` is an ABSOLUTE offset and the transcript keeps growing while the tab is off screen, so restoring it verbatim strands a tail-following tab however far the agent wrote in the meantime - and because the offset is then far above the new bottom, the restore also PAUSES auto-scroll, so the transcript will not even follow the output that stranded them. `initialIsAtBottom !== false` is the real gate (an `undefined` legacy tab counts as at-bottom, matching the unread gate in `useAgentDataListener`); the saved offset is honored ONLY for a tab deliberately parked mid-history, where new entries append below and what the user was reading has not moved. One `requestAnimationFrame` is not enough either - images, web fonts, markdown reflow and code highlighting all grow `scrollHeight` after the first frame, so a deep offset clamps short and the tab opens above where it was left; the restore re-applies under a `ResizeObserver` until it lands or the user takes over.
- **Reaching an off-screen item while dragging:** `useDragAutoScroll(ref, { active, startInset?, endInset? })` in `src/renderer/hooks/ui/useDragAutoScroll.ts`. A drag cannot scroll the container it is over, so an item whose destination is off screen is unreachable without dropping, scrolling, and picking it up again. The hook starts its clock on the first real animation frame rather than at arm time, because the gap between the two is charged as elapsed time and makes the first frame jump. `edgeSize` is the hot band at each end; `startInset` / `endInset` exclude chrome (a pinned button, a fade) that is not part of the scrollable run.
- **Keeping a virtualized list on its selection:** the virtualizer's own `scrollToIndex` from an effect keyed on the selected index, never a `ref` on the selected row. An inline arrow ref is a new identity every render, so React re-attaches it (and re-fires it) on every render rather than on every change of selection - and since `@tanstack/react-virtual` re-renders inside `flushSync` on every scroll-offset change, that closes a loop: a wheel tick scrolls the list, the ref re-attaches, and `scrollIntoView` snaps it back inside the same event. `FileSearchModal`'s wheel was never broken; the row ref was undoing it. `scrollIntoView` also cannot reach a row outside the rendered window, and `behavior: 'smooth'` over thousands of rows animates long enough for the next gesture to fight it. In a NON-virtualized list use `useScrollIntoView` (`src/renderer/hooks/ui/useScrollIntoView.ts`), which keys on the value. Its fourth argument picks the scroll behavior: `'smooth'` (the default) for a dropdown stepped one item at a time, `'auto'` for a list the user HOLDS an arrow key on, because key repeat outruns a smooth animation and every repeat cancels the one in flight. See [UI-PATTERNS.md → Scrolling a Virtualized List](docs/agent-guides/UI-PATTERNS.md#scrolling-a-virtualized-list-to-the-selection).
- **Adding a control to the Left Bar header:** the row is three zones - identity (wand + wordmark, `shrink-0`), indicators (`flex-1 justify-center min-w-0`, `data-testid="sidebar-header-indicators"`), menu (`shrink-0`). A status control - the trophy badge, the now-playing pill, the LIVE toggle, whatever comes next - goes in the CENTER band, never beside the wordmark; `flex-1` is what centers it between the two fixed zones. The row neither wraps nor scrolls and the sidebar goes down to 256px, so it has a declared yield order rather than whatever CSS shrinks first. The MAESTRO wordmark is drawn IN FULL or dropped entirely (`showWordmark` in `SessionList.tsx`); it must never `truncate`, because a brand rendered as "MAE..." reads as a rendering bug. The now-playing pill is the shrink target of last resort - its filename is the one thing here that can be clipped without looking broken, so the pill is `min-w-0` while every button, icon, and divider stays `shrink-0`. Reserve width for the form a control is CURRENTLY in (the pill sheds its filename when narrow, so its compact and labeled reserves are separate constants), and ask `selectNowPlayingVisible` whether the pill is on screen rather than re-deriving it. A reserve is charged only against what is still DRAWN: the LIVE label adds `headerBadgeWidth` only while `showWordmark` is true, because once the wordmark drops out the ~110px it vacated more than covers the badge, and charging anyway left a 256px sidebar showing a bare radio dot with empty space beside it. See [UI-PATTERNS.md → Left Bar Header Width Gates](docs/agent-guides/UI-PATTERNS.md#left-bar-header-width-gates).
- **Element width for JS-computed layout:** `useElementWidth(ref, enabled?)` in `src/renderer/hooks/ui/useElementWidth.ts`. Reach for it only when the value must exist in JS - an inline SVG chart needs real pixels for its viewBox, and a breakpoint that switches column counts needs a number to compare. Anything expressible in CSS stays in CSS. It returns 0 until the first measurement, so gate width-dependent children on `width > 0` rather than drawing a zero-width chart, and it no-ops without `ResizeObserver` so jsdom tests need no polyfill.
- **Usage Dashboard card tile:** `<EntityTile>` in `src/renderer/components/UsageDashboard/EntityTile.tsx` - the shared tile behind every dashboard card grid (agent grid, per-tab grid): status dot, title, badges, corner age, subtitle, labeled stat row, sparkline, plus all four border states and the staggered `card-enter` animation. A new grid shapes data into `EntityTileStat[]`; it does not re-derive the chrome. Not the same thing as the widget library's `StatCard` (one headline metric), and it stays under `UsageDashboard/` because widgets may not import from there.
- **A label that must not truncate:** `useOptionalLabelFits(rowRef)` in `src/renderer/hooks/ui/useOptionalLabelFits.ts`
- **Usage Dashboard metric tile:** `MetricCard` in `src/renderer/components/UsageDashboard/MetricCard.tsx`
- **Recording wizard usage:** `beginWizardRun()`, `recordWizardDocuments()` in `src/renderer/services/wizardStats.ts`
- **Creating a tab straight into a tile:** `tileNewTab()` / `canTileNewTab()` in `src/renderer/hooks/tabs/tileNewTab.ts` - creates an AI/file/terminal/browser tab and splits the current view around it in one session update (the palette's "Tile New ... Below" family). It reuses the drag path's `tileTabIntoGroup` / `createGroupFromDrop`, so keyboard and drag produce identical layouts. Do NOT hand-roll "create a tab, then tile it": the ordinary new-tab paths all clear `activeGroupId` to claim the panel, which tears down the group you are building - that is why this mints every tab non-activating.
- **Opening a modal from inside Settings:** `launchFromSettings(open)` in `src/renderer/utils/launchFromSettings.ts`. Settings renders at `z-[9999]`, so a launcher inside it (Extensions -> "Open Pianola", a plugin's `modal` panel) opens the new modal BEHIND Settings and the click looks like a no-op. This closes Settings first, then runs the launcher. Not for confirmations or pickers that are meant to stack on top of Settings. See [UI-PATTERNS.md](docs/agent-guides/UI-PATTERNS.md#modals-launched-from-inside-settings-launchfromsettings).
- **Layout and navigation for the Git Log graph:** `buildGitGraphCore()`, `computeGitGraphGeometry()`, `stepGitGraphVertical()`, `stepGitGraphHorizontal()`, `jumpGitGraphVertical()`, `gitGraphColumnEdge()`, `gitGraphTopCommit()`, `buildGitGraphTemplate()`, `assignGitGraphBranches()`, `pickBranchFromRefs()` in `src/renderer/utils/gitGraphLayout.ts`. `GitGraphView` DRAWS the graph and `GitLogViewer` NAVIGATES it with the arrow keys, and both go through the SAME `buildGitGraphCore()` - navigation here is VISUAL, so it moves by the layout @gitgraph actually produced (`getRenderedData()` gives every commit an `x` and `y`), never by an order re-derived beside the renderer. That re-derivation is the bug this replaced: @gitgraph orders its columns by when each branch first appears in ITS OWN iteration, so a hand-written lane list sent Left/Right to a column somewhere else on screen and the user lost their place. In its coordinates a column IS a branch (each gets a distinct `x`) and `y` GROWS DOWNWARD with age, so the newest commit is at the top of its column. **Each axis answers one question:** Up/Down walk `commitsInColumn` - the anchor's OWN column, newest first - and Left/Right are the only keys that cross to another branch, landing on the commit nearest the CURRENT HEIGHT (ties resolve upward) so the selection moves sideways instead of teleporting to some branch's tip. Do NOT move vertically by the global commit order: that drifts sideways on its own, which both leaves Left/Right with nothing to do and makes Home/End/PageUp/PageDown land on a branch the user never asked for (they are column ends too, via `gitGraphColumnEdge` / `jumpGitGraphVertical`, and a jump CLAMPS to the end where a single step holds). A commit's message carries the same `onClick` as its dot (`onMessageClick`), because the 5px dot is otherwise the only target and clicking the text a user is reading does nothing. `GitGraphView` also scrolls the selection back into view, since a 200-commit graph runs well past the viewport in both axes and an unseen selection is indistinguishable from a dead key. **The @gitgraph core must NOT know which commit is selected:** `<Gitgraph>` reads its `graph` prop only in its constructor, so a core rebuilt per keypress remounts the SVG, and the momentarily empty scroll container has its offsets clamped to zero - the graph snaps back to the newest commit on every arrow key and the whole feature reads as broken. Selection rides a context consumed by the view's own `renderDot`, and the `<Gitgraph>` key is derived from the core's CONTENTS so new commits still remount. That dot renderer is also what makes a commit clickable at all: it draws a transparent hit circle several times the 5px dot, concentric with it at `(dot.size, dot.size)`, which is where @gitgraph offsets the branch paths to. Note the graph is built from `git log --all` while the List view holds only the current branch, so graph navigation must move by COLUMN rather than by the list's index - a commit selected on a side branch has no list index, and the key reads as dead.
- **Arrow-key navigation over a list or grid:** `useListNavigation()` in `src/renderer/hooks/keyboard/useListNavigation.ts`. Pass `columns` for a 2-D grid (left/right step one item, up/down jump a row); grid mode keys on the option's PRESENCE, so a grid reflowed to one column still answers all four arrows. Omit it for a plain list, where left/right stay inert. For a responsive grid feed it `useGridColumnCount(el, itemCount)` from `src/renderer/hooks/ui/useGridColumnCount.ts`, which takes the ELEMENT (held in state via a callback ref, since a ref mutating in place is invisible to React and would keep measuring an unmounted grid) and reads the RESOLVED `grid-template-columns` - a hard-coded row width walks to the wrong tile as soon as an `auto-fill` grid reflows. Render the result as a roving tabindex over native `<button>`s. See [UI-PATTERNS.md](docs/agent-guides/UI-PATTERNS.md#arrow-navigation-over-a-list-or-grid-uselistnavigation).
- **Rendering raw terminal output (ANSI):** `useAnsiConverter(theme)` in `src/renderer/hooks/ui/useAnsiConverter.ts` for the theme-aware `ansi-to-html` converter, then `getCachedAnsiHtml(text, theme.id, converter)` in `src/renderer/utils/textProcessing.ts` to convert, sanitize (DOMPurify), and cache. The 16 ANSI slots map onto the ACTIVE theme rather than the xterm palette, so do NOT hand-roll another `new Convert({...})` - a second palette drifts the first time a theme adds a color. Collapse carriage returns with `processCarriageReturns()` BEFORE converting, or a progress line arrives as a screen of dead rows, and run any regex over the output through `stripAnsiCodes()` first. The renderer only owns half of this: **nothing Maestro spawns is a TTY**, so the producing side must force color too (`-c color.ui=always` for git, `FORCE_COLOR=1` / `CLICOLOR_FORCE=1` for whatever its hooks run) or the console renders a perfect wall of gray.
- **Any CLI verb that can move the Maestro view:** `resolveBackgroundFlag()` (CLI side) / `readBackgroundField()` (handler side) in `src/shared/focusPlacement.ts`. **Focus belongs to the human operator**: an agent may create a surface, it may not decide the human should be looking at it. `background: true` means the active AGENT does not change AND the active TAB inside any agent does not change - the surface is still created and addressable, so "created but invisible" is a different bug. **The flag is ADDITIVE: no verb's default changed.** `open-file`, `open-terminal`, `open-browser`, `tab new`, `create-agent`, `create-worktree` and `switch-mode` all still focus when the flag is absent; `dispatch --new-tab` is still background-by-default with `--focus`, which is also its pre-existing behaviour. `focus-agent`, `send --tab` and `open` exist TO move the view and carry no flag at all. `readBackgroundField` is an OPT-IN - only a literal `true` counts. **Never write `message.background !== false` in a handler**: that reads an absent field as an opt-in and silently stops the verb from focusing, breaking every existing caller at once. Defaults are keyed by VERB, not message, because `tab new --prompt` and `dispatch --new-tab` share `new_ai_tab_with_prompt` and disagree. `--no-switch` on `open-file` is NOT a spelling of `--background`: it stays on the current agent but still activates the tab there, while `--background` changes nothing rendered anywhere (both may be passed; background wins). On the renderer, background placement is simply not spreading a focus patch - `createTab({ activate: false })`, `addTerminalTab(s, tab, { activate: false })`, `handleOpenFileTab(file, { activate: false })`. See [CLI-UI-PARITY.md → Focus belongs to the human](docs/agent-guides/CLI-UI-PARITY.md#focus-belongs-to-the-human---background----focus).
- **Making a tab the visible one:** the four `*FocusFields` helpers in `src/renderer/utils/tabFocusFields.ts` (`aiTabFocusFields`, `fileTabFocusFields`, `browserTabFocusFields`, `terminalTabFocusFields`), re-exported from `tabHelpers.ts`. The main window renders exactly ONE tab type by precedence (terminal > file > browser > ai), so "focus this tab" is never one assignment - missing a higher-ranked selection leaves the previous view on screen and the focus silently does nothing. They live in their own import-free module because both `tabHelpers` and `terminalTabHelpers` need them and those two already point at each other.
- **Record view for one table row:** `<RecordDetailModal>` in `src/renderer/components/ui/RecordDetailModal.tsx` - the field/value flip of a row that every tabular preview opens on double-click (CSV/TSV via `CsvRowDetailModal`, parquet via `ParquetViewer`). Callers pass a flat `fields` list plus their own `priority` / `resizeKey` / `testIdPrefix`; the mapping from a row to fields belongs in the caller, since a CSV row is positional strings and a parquet row is typed values. Do NOT hand-roll a second one - focus starts on the FIELD LIST rather than the filter input, because Left/Right step rows and Up/Down scroll and none of that works while a text input owns the caret (`/` moves focus to the filter).
- **Previewing a parquet file:** `<ParquetViewer>` in `src/renderer/components/ParquetViewer/`, over the main-process engine in `src/main/parquet/` and the shared filter language in `src/shared/parquet/`. Parquet is the one previewable format that never crosses IPC: `fs:readFile` short-circuits it to a marker (`shared/parquet/preview.ts`) and the viewer queries windows of rows through `window.maestro.parquet.*`. Three invariants: **filtering and sorting round-trip to the engine** (filtering the loaded page would only ever search the first few hundred rows of a hundred million); **`matchedRows` is a LOWER BOUND until `complete` is true**, rendered `1,204+`, converged by a background `countAll: true` pass that doubles as the prefetch; and **hiding a column changes the projection**, so the engine stops decoding it. The one thing that silently loses data: **filter literals compile into the CONVERTED domain, not the physical one** - hyparquet converts footer statistics while parsing, so a `TIMESTAMP(MICROS)` bound is epoch MILLISECONDS, and compiling microseconds prunes every row group in the file while looking healthy. See [REMAINING-SYSTEMS.md → Parquet Query Engine](docs/agent-guides/REMAINING-SYSTEMS.md#5-parquet-query-engine-srcmainparquet-srcsharedparquet).
- **Making a tab the visible one:** the four `*FocusFields` helpers in `src/renderer/utils/tabHelpers/focusFields.ts` (`aiTabFocusFields`, `fileTabFocusFields`, `browserTabFocusFields`, `terminalTabFocusFields`), re-exported from `tabHelpers`. The main window renders exactly ONE tab type by precedence (terminal > file > browser > ai), so "focus this tab" is never one assignment - missing a higher-ranked selection leaves the previous view on screen and the focus silently does nothing. They live in their own import-free module because both `tabHelpers` and `terminalTabHelpers` need them and those two already point at each other. `src/renderer/utils/tabFocusFields.ts` is a thin re-export of the same four for older import sites.
- **Modal layer:** `useModalLayer()` in `src/renderer/hooks/ui/useModalLayer.ts` (do NOT use manual `registerLayer()` boilerplate)
- **Keyboard-shortcut hint / kbd pill:** `<Keycap>` and `<KeycapHint>` in `src/renderer/components/ui/Keycap.tsx`. Draws a key as a physical key (face, border, bottom lip) instead of a bare glyph in a caption. `pressed` sinks the cap so a surface can echo the real keypress - drive it from a short timer, never from `keyup`, because a held key repeats without ever sending one. `onClick` makes the hint the control itself, which is how a legend can double as the Apply / Cancel row and still satisfy the graphical-exit rule. Do NOT hand-roll another `<kbd>` pill. See [UI-PATTERNS.md -> Keycaps](docs/agent-guides/UI-PATTERNS.md#keycaps-keycap--keycaphint).
- **Modal / find-bar ESC pill:** `<EscCloseButton>` in `src/renderer/components/ui/EscCloseButton.tsx`. Every modal, palette, and find bar needs a graphical exit - Escape alone strands users on remote desktop and tablets. Pass an `onClose` that does exactly what the Escape handler does (extract it into a shared `useCallback` rather than duplicating the body). Do NOT hand-roll the `px-2 py-0.5 rounded text-xs font-bold` pill; the inert `<div>` version used to be copy-pasted in nine places. See [UI-PATTERNS.md → Every Modal Needs a Graphical Exit](docs/agent-guides/UI-PATTERNS.md#every-modal-needs-a-graphical-exit-escclosebutton).
- **Keeping a floating menu on screen:** `useContextMenuPosition(menuRef, x, y)` in `src/renderer/hooks/ui/useContextMenuPosition.ts`, which `useAnchoredMenuPosition` also passes through. It returns `maxHeight` beside `left`/`top`, and **a menu whose content is unbounded must apply BOTH `maxHeight` and `overflowY: 'auto'`**. Clamping position alone is not enough and fails silently: for a menu taller than the viewport the clamp computes a NEGATIVE `maxTop`, so `top` pins to the padding and every item past the bottom edge is unreachable - these containers are `overflow-hidden`, and a context menu does not scroll the page behind it. The file tree's menu carries up to ~27 entries, which overflows a phone in portrait and a laptop in a short window. The budget is measured from where the menu LANDS, not from the click, because a short menu near the bottom is pushed up and has more room than `y` suggests. Do NOT substitute a viewport-relative cap like `maxHeight: 'calc(100vh - 1rem)'`: a menu anchored low still runs off the bottom with nothing to scroll it. Do NOT drop the sibling `opacity: ready ? 1 : 0` when adding these - it is what stops the menu flashing at the pre-measurement position.
- **Dropdown/tooltip anchored to a header element:** `useAnchoredMenuPosition(menuRef, anchorRef)` in `src/renderer/hooks/ui/useAnchoredMenuPosition.ts`, rendered via `createPortal(..., document.body)`. Anything anchored inside the Main Panel header MUST use this. `absolute top-full` is silently clipped: the header wraps its left cluster in `overflow-hidden` boxes only as tall as the pill, which is how the git pill's hover card and the git status tooltip were both invisible for months. Bare `position: fixed` doesn't help either, because `.header-container` sets `container-type: inline-size` (implying `contain: layout`), making the header a containing block for fixed descendants. Since jsdom has no layout engine, a test asserting `toBeInTheDocument()` passes while the element is invisible - assert it is NOT a descendant of the header subtree instead.
- **Remembered textarea height:** `useResizableTextarea()` in `src/renderer/hooks/ui/useResizableTextarea.ts` - every `resize-y` textarea should keep the height the user dragged it to, across reopen and app restart. Spread its `style` last and pass `externalRef` if the component already owns a ref. See [UI-PATTERNS.md → Resizable Textareas](docs/agent-guides/UI-PATTERNS.md#resizable-textareas).
- **Auto-growing composer textarea:** `useAutosizeTextarea()` in `src/renderer/hooks/ui/useAutosizeTextarea.ts` (helpers in `src/renderer/utils/textareaSizing.ts`). Every composer that grows with its content rides it - AI composer, both wizard composers, group chat, feedback chat. Do NOT hand-roll `height = 'auto'` then `height = scrollHeight` again: the `'auto'` toggle collapses the internal scroll to the top, so a full, scrolling composer clipped the line being typed on every keystroke. Run it on the committed value, never inside `onChange` - an `onChange` resize never fires for dictation, draft restore, or template insertion, which grows the text without growing the box. Distinct from `useResizableTextarea`, which remembers a height the USER dragged.
- **Line numbers beside a `<textarea>`:** `<TextareaLineNumbers>` + `lineNumberGutterMetrics()` in `src/renderer/components/ui/TextareaLineNumbers.tsx`. The gutter is scroll-synced (translated by the textarea's `scrollTop`, written straight to the DOM so a fast scroll can't lag a frame) and wrap-aware (each logical line is measured against a hidden mirror sharing the textarea's font and wrap width, so number N sits on the FIRST visual row of line N). Both the Cue YAML editor and the Auto Run expanded modal ride it. Do NOT hand-roll another `value.split('\n').map((_, i) => <div>{i + 1}</div>)` gutter - that's what the YAML editor had, and it drifted out of alignment the moment the document scrolled or any line wrapped. Derive the textarea's `paddingLeft` from `lineNumberGutterMetrics(value).textPaddingLeft` rather than guessing a pixel value; the metrics are in `ch` and reserve two digits minimum so the editor doesn't reflow at line 10. See [UI-PATTERNS.md → Line Numbers on a `<textarea>`](docs/agent-guides/UI-PATTERNS.md#line-numbers-on-a-textarea-textarealinenumbers).
- **Guarded `localStorage` access:** `safeLocalStorage()` in `src/renderer/utils/safeLocalStorage.ts`, with `safeSessionStorage()` beside it for state that belongs to one browser TAB rather than to the browser, `writeStorageValue(storage, key, value)` for a write to any Storage, and `safeStorageGet(key)` / `safeStorageSet(key, value)` for ordinary localStorage preference reads and writes. Reading the global itself can THROW (storage-blocked renderer, private mode, a jsdom test without Storage), so never touch `localStorage` directly in a hook - three private `storage()` copies existed before this was pulled out. The accessors only cover REACHING the object: `getItem` / `setItem` still throw on a denied or quota-exceeded origin, which is what the get/set pair and `writeStorageValue` swallow. Keep `safeLocalStorage()` for `removeItem` / `key` / enumeration. A hostile Storage must cost the user their persistence, not their pane.
- **A view preference a user sets by clicking:** `usePersistedToggle(storageKey, defaultValue)` in `src/renderer/hooks/ui/usePersistedToggle.ts` - one boolean in localStorage, for a collapsed banner or a folded section that must survive the surface unmounting (a Right Bar tab switch, a re-render from new data) but isn't worth a Settings row. Storage failures degrade to in-memory only, so a storage-blocked renderer costs the user persistence, not their pane. Do NOT hand-roll another `useState(() => localStorage.getItem(...) === 'true')` pair - a collapse that resets on every re-render reads as the banner refusing to stay closed. `AutoRunNoticeBanner`'s `collapseKey` is the first caller. For a preference that is one of several words rather than yes/no (a sort mode, a view density), use `usePersistedChoice(storageKey, options, defaultValue)` in the same folder: it validates the stored string against the option list, so a mode from an older build falls back to the default instead of stranding the surface in a state its control cannot express. See [UI-PATTERNS.md → Collapsible Advisories](docs/agent-guides/UI-PATTERNS.md#collapsible-advisories-autorunnoticebanner-usepersistedtoggle).
- **Sizing a Document Graph node:** `calculateNodeWidth(label, previewCharLimit)` and `calculateNodeHeight(previewText, previewCharLimit)` in `src/renderer/components/DocumentGraph/mindMapLayouts.ts`. Do NOT space a layout off the `NODE_WIDTH` constant: with previews off a document draws as a filename pill about a third of a card wide and a fraction of its height, so reserving the card width claims roughly five times the real footprint. That is what made the radial rings thousands of pixels across, and it reached the force layout too, whose `forceCollide` radius read the same constant. The layout vocabulary itself (`MindMapLayoutType`, `LAYOUT_LABELS`, `MIND_MAP_LAYOUT_TYPES`, `nextMindMapLayout`, `isMindMapLayoutType`) lives in `DocumentGraph/layoutTypes.ts`, a leaf module with no d3 import so `settingsStore` and the `Session` interface can name a layout without pulling in the engine - it had been written out three times, and a layout added to the graph was silently rejected on the way to disk by a copy nobody updated.
- **A pane width the user sets by dragging:** `useResizablePanel()` in `src/renderer/hooks/ui/useResizablePanel.ts` for the drag itself, then pick the persistence by where the pane lives. Top-level chrome (Left Bar, Right Bar) is a real setting: pass `settingsKey` and back it with a `settingsStore` field. A pane INSIDE another surface (the Document Graph's preview pane, a split inside a panel) is a view preference: pair it with `usePersistedPanelWidth(storageKey, { defaultWidth, minWidth, maxWidth })` and OMIT `settingsKey`, or the hook writes the width a second time under a key nothing reads back. Keep the stored bounds and the live clamp separate - the stored bounds decide what may be written to disk, while the `maxWidth` passed to the drag folds in the container as it is right now, so a width that was legal on a maximized window can't swallow the canvas after the modal is resized down.
- **A modal default sized to the screen:** `viewportModalSize({ width, height })` in `src/renderer/utils/modalSizing.ts`, passed as `defaultSize` to `useResizableModal`. Only for surfaces the user pans around INSIDE (a graph, a dashboard) - a form or dialog has a natural width and more room buys it nothing. A fixed default that reads as generous on a laptop is a postage stamp on a 5K display, and the user re-drags it on every machine. Memoize it once per mount: the hook re-clamps the live size on `resize`, and a default that moves under it fights that listener.
- **Focus after render:** `useFocusAfterRender()` in `src/renderer/hooks/utils/useFocusAfterRender.ts` (do NOT use `useEffect + setTimeout(() => ref.focus())`). Same file: `useFocusOnMount()` for a modal that must land with its input focused, and **`useFocusOnClose(ref, isOpen)` for handing the caret BACK when a transient surface closes**. The layer stack restores focus generically now (`useLayerStack` snapshots `document.activeElement` when a focus-capturing layer registers and hands it back when the layer unregisters, but ONLY if focus landed nowhere), so a modal registered through `useModalLayer` needs nothing. Reach for `useFocusOnClose` when the surface is NOT a registered layer (an inline editor, a popover) or when the caret should land somewhere OTHER than where it started - it wins, because the stack defers to anything that already claimed focus. It fires only on the true -> false edge, so it cannot steal focus from something the user clicked afterwards. Do NOT restore focus by hand in a modal's `onClose`: a surface dismissed with Escape never runs it, which is how focus ended up on `document.body` with the pane behind it looking ready while swallowing the next keystroke.
- **Event listeners:** `useEventListener()` in `src/renderer/hooks/utils/useEventListener.ts` (do NOT pair raw `addEventListener`/`removeEventListener` inside useEffect)
- **Debounce/throttle:** `useDebouncedValue()`, `useDebouncedCallback()`, `useThrottledCallback()` in `src/renderer/hooks/utils/useThrottle.ts` (filename is misleading - all three live here)
- **Identity-stable callback:** `useStableCallback()` in `src/renderer/hooks/utils/useStableCallback.ts` - one identity forever, always calling the newest implementation. Reach for it when a new callback identity is read as a STRUCTURAL change rather than a fresh value. The canonical case is `createMarkdownComponents()`: it builds a fresh map of component functions, so rebuilding it gives React a new component TYPE for every element and it unmounts and remounts the entire rendered document, discarding the reader's scroll position and restarting images and Mermaid. Any callback that closes over document content (a task-checkbox toggle, for instance) is reborn on every edit and triggers exactly that. Do NOT use it to silence an exhaustive-deps warning - it hides the callback from dependency arrays, so an effect that should re-run when its captured values change no longer will.
- **Render markdown:** `<Markdown preset="chat | document | wizard-bubble | release-notes">` from `src/renderer/components/Markdown/` (do NOT hand-roll `<ReactMarkdown>` + a per-surface `components`/plugin map). The chat preset is what `MarkdownRenderer` wraps. Shared internals: `buildMarkdownPlugins` (`Markdown/plugins.ts`), `preprocessMarkdown` (`Markdown/preprocess.ts`), leaf renderers in `Markdown/components/*`, and the document component map `createMarkdownComponents()` in `src/renderer/utils/markdownConfig.ts`. See [UI-PATTERNS.md → `<Markdown>`](docs/agent-guides/UI-PATTERNS.md).
- **Clickable task checkboxes in rendered markdown:** `toggleTaskCheckboxAtLine(content, line)` in `src/renderer/utils/markdownTasks.ts`, plus `rehypeSourceLine` (`src/renderer/components/Markdown/rehypeSourceLine.ts`) and the `onTaskToggle` option on `createMarkdownComponents()`, which swaps in `<TaskCheckbox>`. react-markdown renders every GFM checkbox `disabled`, so those three pieces are what make one clickable - the file preview and the Auto Run panel both ride them. Do NOT count checkboxes in the DOM and map them onto the Nth `- [ ]` line: that drifts the moment a task marker appears inside a code fence. **Every component override in `createMarkdownComponents()` must forward its props** (destructure `node` out, spread the rest): `rehypeSourceLine` tags EVERY block, and `lineSync.domGetTopLineByAttr()` reads those tags to keep the file preview and the editor on the same line across the toggle. `React.createElement('p', null, children)` eats the attribute, which left HEADINGS as the only anchored blocks - so a document scrolled to the very top reported the first heading, and hitting Edit jumped there. See [UI-PATTERNS.md → Clickable task checkboxes](docs/agent-guides/UI-PATTERNS.md#clickable-task-checkboxes) and [Preview/edit scroll sync](docs/agent-guides/UI-PATTERNS.md#previewedit-scroll-sync-rides-the-same-data-source-line-tags).
- **Model/effort badges on a finished turn:** `<TurnSettingPills>` in `src/renderer/components/ui/TurnSettingPills.tsx`, fed from `LogEntry.turnModel` / `turnEffort`. Those are stamped at SEND time by `codifyTurnSettings()` in `src/renderer/utils/providerTabSessions.ts` - spread it into whatever tab patch marks the tab busy. Never label a turn from the live tab or agent value: settings are codified at send, so a model change made mid-stream must not relabel the response already running. **A QUEUED message freezes its settings when it is queued**, since the queue may not drain until several model changes later: every path that builds a `QueuedItem` spreads `captureQueuedTurnSettings()` into `item.turnSettings`, and both the pills (`markTabRunningQueuedItem`) and the spawn (`agentStore.processQueuedItem`) read it back through `codifyQueuedTurnSettings()`. The presence of the object is the capture flag - an undefined model INSIDE it means "the agent default was chosen", so never `?? ` it against the live value. See [UI-PATTERNS.md → Turn Attribution Pills](docs/agent-guides/UI-PATTERNS.md#turn-attribution-pills-turnsettingpills).
- **Tab overlay-menu shortcut badge:** `<ShortcutHint keys={...} theme={theme} />` in `src/renderer/components/ui/ShortcutHint.tsx` - the same component the entry above describes, and the only copy. Every tab item's overlay menu (AI, file, terminal, browser, group) uses it; it was previously re-declared inline, byte-identical, in four components, and then a second time as a TabBar-local module. Do NOT re-declare it locally or hand-roll the `ml-auto` badge span.
- **Which tab a tab-scoped action targets:** `resolveActiveTabRef(session)` in `src/renderer/utils/panelLayout.ts` returns the `UnifiedTabRef` a per-tab command (rename, etc.) should act on - the active tiled group's FOCUSED PANE when a group owns the panel, else the single-view tab via `resolveSingleViewTabRef()`. Do NOT hand-roll the `terminal -> file -> browser -> AI` precedence chain: focusing a tiled pane does not set `inputMode`/`activeTerminalTabId`, so an open-coded chain silently targets the AI tab hidden behind the group. Pair with `resolveTabRefRenameValue()` (user-assigned name only, `null` when the tab is gone) to seed a rename input, and `resolveTabRefTitle()` for display text.
- **The tab a queued item is going to:** `resolveQueuedItemTabName(session, item)` in `src/renderer/utils/executionQueue.ts` for the LABEL, `resolveQueuedItemTarget()` in `src/renderer/utils/tabHelpers` for the tab itself. Unlike `turnSettings`, a `QueuedItem`'s `tabName` is NOT frozen on purpose - it is a last-known fallback for a tab that is gone by the time the queue drains. Do NOT render `item.tabName` directly: an item queued into a brand-new tab snapshots `New` and keeps it forever, so after auto-naming the Execution Queue indicator grouped one tab under whichever name its FIRST item happened to carry, and the browser listed two rows for one tab under two different names. The resolver reads the live tab, then a closed-but-draining orphan, then the snapshot. See [UI-PATTERNS.md → Queued Item Tab Labels](docs/agent-guides/UI-PATTERNS.md#queued-item-tab-labels-resolvequeueditemtabname).
- **Editing the newest queued message:** `requestEditLastQueuedMessage()` in `src/renderer/services/editQueuedMessage.ts`. Two surfaces offer it (the `editLastQueuedMessage` chord and the palette's `Edit Last Queued Message`) and both route here, so they cannot drift on which item they pick, which tab they land on, or what they say when there is nothing to edit. Everything is read from the stores AT CALL TIME, never from a render snapshot - the pencil on a queued row reads live props, so a stale snapshot is the one way this can claim nothing is queued while a card sits on screen. A missing tab RANKS rather than filters (a closed tab must not turn into "nothing is queued"), only `type === 'command'` items are skipped, and the two empties are named apart: `Nothing queued to edit` vs `Only commands are queued`. See [RENDERER-SERVICES.md](docs/agent-guides/RENDERER-SERVICES.md#editqueuedmessagets---edit-the-newest-queued-message).
- **Whether a Force Send control exists at all:** `shouldOfferForceSend(eligibility)` in `src/renderer/utils/executionQueue.ts`, paired with `getForceSendEligibility()` (can it run?) and `getForceSendTitle()` (why not?). Rendering and enabling are separate questions: `canForce` decides DISABLED, this decides PRESENT. Only `needs-forced-parallel` is worth showing dimmed, because its tooltip names a setting the user can go turn on; `no-target-tab` could never work, and `target-tab-busy` resolves itself (a tab runs one turn at a time, so the item is already next in line). Both surfaces - the inline QUEUED card in `QueuedItemsList` and the Execution Queue browser's Send Now - call it, so they cannot drift on when the button appears, which they had already done once in both directions.
- **Model tier / effort level (`'low' | 'medium' | 'high'`):** `resolveTierModel()`, `resolveEffortLevel()`, `cheapTurnSettings()` in `src/shared/modelTiers.ts`; `MAESTRO:MODEL` markers via `findActiveModelHint()` in `src/shared/autorunModelHints.ts`; the join in `resolveTurnSettings()` in `src/shared/autorunTurnSettings.ts`. A marker's PLACEMENT is its scope (own line = from there down, end of a task line = that task only), the two layer per axis, and `'default'` stays a directive rather than collapsing to `undefined` so a task can opt out of a document-wide hint - do NOT normalize it away at parse time. One vocabulary maps to every provider, and it maps to ladder POSITIONS, not same-named strings - Claude's `high` is `max`, Codex's `low` is `minimal`. Do NOT pass a provider's own effort word through from a document, and do NOT add a tier table for a provider whose model IDs are discovered at runtime (codex, copilot-cli, opencode): a shipped guess rots into naming a model the user cannot run. Unmapped returns `undefined`, meaning inherit-the-agent's-value AND warn - never substitute silently, because a deep-planning task quietly running on the default model is the exact failure this replaced. Shared because the desktop and CLI Auto Run engines must resolve a document identically.
- **Un-parking an Auto Run stopped on an error:** `registerBatchResumer()`, `getBatchResumer()` in `src/renderer/services/batchResumer.ts`; `resolveAutoResumePolicy()` in `src/shared/autorunAutoResume.ts`; `scheduleAutoResume()`, `cancelPendingAutoResume()` in `src/renderer/stores/autoRunResumeStore.ts`
- **Auto Run markers (HITL / halt / model hint):** `scanMaestroMarkers()`, `findPendingHitlGate()`, `detectHaltMarker()`, `hasMaestroMarker()` in `src/shared/autorunMarkers.ts`, rendered by `remarkMaestroMarkers` (`src/renderer/components/Markdown/remarkMaestroMarkers.ts`) + `<MarkerPill>`. All three markers are HTML comments, so they render as NOTHING - and two of them silently block the next run (a live HITL gate pauses it, a halt marker makes Auto Run refuse to start), which presents to the user as "I pressed Run and nothing happened". `scanMaestroMarkers` is the rendering side and reports each marker's STATUS (`live` / `spent` / `invalid`), because presence is not the useful question: a gate above an unchecked task and one above a checked task are a character apart in the source and only the first stops the run. Do NOT hand-roll another marker regex - the engines and the pill must agree, or the pill sends the user to remove the wrong marker. Pills are wired ONLY into `createMarkdownComponents` (the document map); chat builds its own map, so an agent explaining the syntax in a message keeps rendering as prose rather than claiming a setting exists. One deliberate asymmetry: `detectHaltMarker` is NOT fence-aware (the engine must never miss a halt an agent mis-indented) while `scanMaestroMarkers` is (a pill on a documentation example would be a lie).
- **Fence-aware markdown scanning:** `forEachMarkdownLine()`, `UNCHECKED_TASK_REGEX`, `CHECKED_TASK_COUNT_REGEX`, `CHECKED_TASK_REGEX` in `src/shared/markdownTaskScan.ts`. Every Auto Run document scanner (task counting, HITL gates, model hints, human-step detection) rides this one walk, in `shared/` because the CLI engine cannot import from `src/renderer`. Do NOT hand-roll another line loop: any scanner that forgets the fence bookkeeping fires on a playbook that merely DOCUMENTS the marker syntax, and the copies drift on closing-fence length, tilde fences, and CRLF.
- **Thinking mode (`'off' | 'on' | 'sticky'`):** `THINKING_MODES`, `nextThinkingMode()`, `asThinkingMode()` in `src/shared/types.ts`. One cycle order serves the composer's Thinking chip and `maestro-cli tab thinking <tab-id> cycle`, so a click and a CLI cycle cannot disagree about what comes next - do NOT hand-roll another `['off', 'on', 'sticky']` ladder or an inline `indexOf(...) + 1` step. Narrow anything arriving from the CLI or the web bridge through `asThinkingMode()` rather than casting: `'off'` is a truthy string, and an unrecognized mode written straight into a tab is a permanently wrong chip rather than a rejected command.
- **Whether an agent is working right now (main process):** `isAgentBusy(session, processManager)` / `isAiTabProcessActive(processManager, agentId, tabId, isActiveTab)` in `src/main/utils/agent-busy.ts`. LIVE liveness, not the persisted state: persistence rewrites every session and tab to `'idle'` on the way to disk, so the stored record can never say whether an agent is mid-turn. It also folds in `isSessionBusyWithCli`, because a playbook run holds the same working directory a Group Chat delegation would hand work to. Do NOT hand-roll another `processManager.get(`${id}-ai-${tabId}`)` probe: the ACTIVE tab also answers to the legacy bare `{agentId}-ai` id that older spawn paths still use, and a copy that forgets it reports a busy agent as free.
- **Loading a LOCAL file tree:** `walkLocalFileTree()` / `loadFileTree()` in `src/main/utils/file-tree-walk.ts`. One main-process walk per refresh instead of a directory-at-a-time IPC round trip from the renderer.
- **`{{template}}` variable autocomplete:** `useTemplateAutocompleteEngine()` in `src/renderer/hooks/input/useTemplateAutocompleteEngine.ts` owns the state machine (when the popup opens, what the query is, which key does what, what text replaces what); the text surface is supplied by a small `TemplateAutocompleteTarget` binding. Two exist: `useTemplateAutocomplete()` for a plain `<textarea>` (Auto Run, the command panels) and `useEditorTemplateAutocomplete()` for the CodeMirror `MarkdownEditor` (Maestro Prompts). Do NOT hand-roll a second `{{` detector for a new editor - write a target for it.

- **Whether a surface is on a phone:** `usePhoneLayout()` / `isPhoneLayout()` in `src/renderer/hooks/ui/useViewportBreakpoint.ts` - the web-desktop bundle at the `xs` breakpoint, with the CSS twin `html[data-runtime='web-desktop'][data-bp='xs']`. THE gate for simplifying a surface on a handheld (icon-only toolbars, full-screen drawers, a sheet instead of a popover, a folded composer). Viewport-driven, never pointer-driven: a tablet has a finger without being short on room, so touch GESTURES gate on `isCoarsePointer()` separately. The native Electron app never reports phone layout. Do NOT hand-roll `window.innerWidth < 640 && isWebDesktop()` - the CSS and JS sides must agree. See [WEB-MOBILE.md → Phone Layout](docs/agent-guides/WEB-MOBILE.md#phone-layout).
- **The `src` of a conversation image:** `displayImageSrc(src)` in `src/renderer/utils/sessionImageSrc.ts`, over the reference grammar in `src/shared/sessionImageRefs.ts`. A persisted image is a `maestro-image://store/<sha>.<ext>` reference: the Electron app loads it through the protocol handler, a browser cannot, and every stored screenshot rendered as a broken-image glyph on the web interface. Every `<img>` that shows a transcript, queue, staged, group-chat, or wizard image runs its src through this; in web-desktop it becomes the token-scoped `/<token>/api/images/<name>` route (`src/main/web-server/routes/imageRoutes.ts`), everywhere else it passes through untouched. The basename grammar is also the traversal guard, shared by the store, the route, and the resolver, so do NOT re-declare the prefix or the regex. A transcript CHIP additionally wraps that result in `sessionImageThumbnailSrc(src, w, h)` from the same module, which appends the `?tw=&th=` query the `maestro-image` protocol handler serves a disk-cached rendition for - a chip is 200x80 CSS px while the source is routinely a 4984x2578 screenshot, and Chromium cannot decode "just the small version". Order matters and is the easy thing to get wrong: `displayImageSrc` runs FIRST, because thumbnails ride the custom scheme and the thumbnail helper no-ops on anything that is not a bare ref, so the desktop gets a rendition while a browser client keeps receiving originals. Appending the query first leaves a `maestro-image://` URL no browser can load. A BARE ref must always serve the original bytes - the lightbox, clipboard copy, and every export path depend on it.
- **A tab chip's action menu shell:** `<TabOverlayPortal>` in `src/renderer/components/TabBar/TabOverlayPortal.tsx`. Anchored popover on desktop, dismissable scrolling bottom sheet on a phone (drawn by the shared `<PhoneBottomSheet>` below), where the anchored menu ran off the bottom of the screen with no way to close it. It used to be copy-pasted, byte for byte, into all five chip components. The menu content is unchanged in both shapes; the sheet restyles it through `.maestro-tab-sheet__body`, and `<ShortcutHint>` carries `data-shortcut-hint` so the sheet can hide every chord badge (no keyboard on a phone). Chips are `<LongPressable>` (tap selects, long-press opens; `innerRef` keeps the chip's own ref) with `draggable` off on coarse pointers, because a long-press is also how the OS starts an HTML5 drag.
- **A bottom sheet on a phone:** `<PhoneBottomSheet>` in `src/renderer/components/ui/PhoneBottomSheet.tsx`. THE shell for any surface that is an anchored popover on desktop and cannot be one at 390px - measured off a 40px control it runs past the bottom of the screen with no way to scroll it and no way to dismiss it. It owns the scrim, the grip-only swipe-down (`useSwipeGestures`; a swipe that starts inside the scrolling body must scroll it, not dismiss the sheet), the close button, the safe-area padding, and the rise animation. `TabOverlayPortal` draws its phone shape with it and `ComposerOptionsSheet` is the second caller, so do NOT `createPortal` another `fixed z-[100] inset-0` shell. Two props carry the whole difference between callers: `bodyClassName` is the caller's own styling hook (`.maestro-tab-sheet__body` flattens a menu written for a mouse; a sheet authored for a phone wants none of that, which is why the rule is scoped rather than living on the shared class), and **`scrimArmMs` must match the gesture that opened it** - a sheet opened by a LONG-PRESS keeps the default `PHONE_SHEET_SCRIM_ARM_MS`, because the browser follows the finger lifting with synthesized mouse and click events at a position the sheet now covers and the sheet would close the instant the user let go, while a sheet opened by a plain TAP passes `0`, since arming it just swallows the user's first real dismissal.
- **Rows inside a `PhoneBottomSheet`:** `PhoneSheetSection`, `PhoneSheetOptionRow`, `PhoneSheetActionRow` in `src/renderer/components/ui/PhoneSheetRows.tsx`, with `PHONE_SHEET_ROW_HEIGHT` (52) and `PHONE_SHEET_OPTION_HEIGHT` (44). `<PhoneBottomSheet>` owns the chrome; these are the only three shapes that go inside it. A multi-choice control is an ACCORDION, never a tap-to-cycle pill - cycling one step per tap is a fine control beside a mouse and a poor one under a finger, since the user cannot see the options, cannot go back, and has to tap through a state they did not want - and only ONE section is open at a time, owned by the caller, because a second open list sits below the fold with no sign it is there. The heights are constants because they must clear the 44px tap-target floor `src/web/index.css` sets on every button under 767px. Do NOT re-declare a local `OptionSection` / `OptionRow` in a third sheet.
- **The file preview toolbar on a phone:** `<FilePreviewActionsSheet>` in `src/renderer/components/FilePreview/FilePreviewActionsSheet.tsx`, fed by the `FilePreviewHeaderAction` / `FilePreviewHeaderStat` lists `FilePreviewHeader` builds. The desktop header's icon cluster, path line, and stats subbar ate roughly 400px of an 844px phone before a word of the file was readable, so there the header keeps the file name, Save, and one "..." that opens this sheet. **The actions are not restated in the sheet** - one ordered list is rendered as icon buttons by the header and as labelled rows by the sheet, so every `toolbarVisibility` gate is applied once for both; a second copy would hand back a button the user went to Settings to remove. Save stays inline on both (a labelled primary control with a dirty state must not go two taps deep). The preview tier becomes a `PhoneSheetSection` accordion rather than `PreviewTierChip`'s anchored popover, which the scrolling sheet body would clip.
- **Every composer setting on a phone:** `<ComposerOptionsSheet>` in `src/renderer/components/InputArea/components/ComposerOptionsSheet.tsx`, opened by the `...` in `ToolbarControls`' phone branch. The desktop toolbar spreads History / Access / Thinking / Model / Effort across a row of pills that cannot fit at 390px, so the phone row keeps only what a thumb reaches for mid-sentence - attach, mic, send (centre), `...` - and the send button moves here from the column beside the composer while the prompt-composer pencil and the notification bell are dropped outright (both open surfaces of their own). Everything in the sheet is an ACCORDION, never a tap-to-cycle pill: cycling one step per tap is a fine control beside a mouse and a poor one under a finger, since the user cannot see the options, cannot go back, and has to tap through a state they did not want. That is why the "set this mode" halves live beside the cycle halves in `tabHelpers/focusFields.ts` - `permissionModeFields()` / `nextPermissionMode()` and `setShowThinkingFields()` (which `cycleShowThinkingFields` now delegates to) - so the sheet and the toolbar cannot drift on keeping `readOnlyMode` in lockstep with `permissionMode`, or on dropping thinking logs when the mode goes off. Only one section is open at a time: the panel is `50dvh`, so a second open list sits below the fold with no sign it is there.
- **A keyboard-only hint (chord badge, keycap, arrow-key legend):** put `data-shortcut-hint` on it. One rule in the "Phone layout" block of `src/renderer/index.css` hides every element carrying it on a phone, where there is no keyboard to press and where those badges were the first things to push a close button off a 390px screen. `<ShortcutHint>` sets it already; the hamburger badges, the palette row chords, the Settings search keycap, and the `↑↓ navigate` footers all carry it. Do NOT gate a hint on `usePhoneLayout()` in JSX - a hint that forgets the attribute is the only way this drifts.
- **A modal that fills a phone:** nothing to write. Every modal that passes a `resizeKey` stamps `data-modal-resize-key`, and the phone stylesheet makes those full-screen, hides their `[data-resize-handle]` grips, and zeroes the overlay padding. A small dialog (confirm, rename) passes no key and stays a dialog. Do NOT add a per-modal phone branch for sizing; the phone branches in `TabSwitcherModal` and `CrossTabSearchModal` predate the rule and also drop content, which is why they remain.
- **Clearing the iOS status bar in a home-screen web app:** `--maestro-top-inset` in `src/renderer/index.css`, fed by `installStandaloneStatusBarInset()` in `src/renderer/utils/standaloneStatusBar.ts` (called from the web bootstrap). Opened from the Home Screen, the page runs UNDER the status bar and iOS 26 draws a frosted layer over that band that also swallows taps, so a header laid out at `y = 0` is dimmed and dead - which is what made the hamburger unreachable and the edge swipe the only way to the menu. `env(safe-area-inset-top)` is the right answer where WebKit reports it, and 0 where it does not (WebKit bug 301994 shortens the viewport by the bar height instead), so the property is the `max()` of `env()` and the JS-measured `screen.height - innerHeight`. Do NOT hard-code a status bar height and do NOT gate on `display-mode: standalone` - only `navigator.standalone` says iOS, and an Android PWA never extends under its bar. Related: the web-desktop roots are `100dvh`, not `100%`, because `100%` is the SMALL viewport (`100svh`) in a home-screen web app and clipped the `100dvh` shell at the status-bar line. The SAME installer also publishes `--maestro-viewport-height` from `measureVisibleViewportHeight()` (`visualViewport.height`, falling back to `innerHeight`), and every FULL-SCREEN PHONE MODAL is sized to it rather than to `100dvh`: on iOS the on-screen keyboard slides OVER the layout viewport instead of shrinking it, so a `100dvh` sheet keeps its full height with its bottom ~45% behind the keys. That is invisible until a surface autofocuses a field on open, which is why the command palette and the tab switcher now gate their mount focus on `usePhoneLayout()` - both were burying the list the user opened them to scroll. See [WEB-MOBILE.md -> Standalone mode](docs/agent-guides/WEB-MOBILE.md#standalone-mode-ios-home-screen).
- **Swipe down to close a modal on a phone:** `useLayerSwipeDismiss()` in `src/renderer/hooks/ui/useLayerSwipeDismiss.ts`, mounted once in `LayerStackProvider`. It rides `closeTopLayer()`, the same path Escape takes, so dirty-state confirmation and nesting behave identically. Deliberately conservative: web-desktop with a coarse pointer only, only while a layer is open, the gesture must start in the top quarter of the viewport, not from a form control, not inside a region that is already scrolled down, and travel at least 80px mostly vertically within half a second. A surface that pans on drag (a canvas, a graph) opts out with `data-no-swipe-dismiss` on its root. Do NOT add per-modal swipe handlers.
- **Whether a configured env value means "unset":** `isBlankEnvValue()`, `stripBlankEnvVars()` in `src/shared/agentEnvironment.ts`
- **Reaching the open file preview from a modal:** `requestHeadingPalette()`, `HEADING_PALETTE_EVENT` in `src/renderer/services/headingPalette.ts`
- **Rendering a surface containing `<MarkdownEditor>` in a test:** `markdownEditorModuleMock()` in `src/__tests__/helpers/mockMarkdownEditor.tsx` (CM6 cannot lay itself out in jsdom)
- **Sizing a virtualized row the user's font decides:** `virtualizer.measureElement` + `data-index` and NO inline `height`; `HistoryPanel`, `FileSearchModal`
- **Diagram content clipped at the SVG edge:** `expandSvgViewBoxToContent(svg, padding?)` in `src/renderer/utils/svgViewBox.ts`
- **Sizing a surface to its longest label:** `widestLabelWidth(labels, opts)`, `estimateLabelWidth()` in `src/renderer/utils/labelWidth.ts`
- **Ordering the Auto Run run list from the picker:** `applySelectionOrder()`, `selectFolderFiles()` in `src/renderer/utils/documentSelectionOrder.ts`

If your use case does NOT match an existing utility, prefer extending the canonical file over creating a new one. If you genuinely need something new, add the full entry to [CANONICAL-UTILITIES.md](docs/agent-guides/CANONICAL-UTILITIES.md) and a one-line index entry above so the next person can find it.

The tracker at [DEDUP-TRACKER.md](docs/agent-guides/DEDUP-TRACKER.md) lists all known duplication findings.

---

## Agent Behavioral Guidelines

Core behaviors for effective collaboration. Failures here cause the most rework. These are judgment calls, not scripts: state what you decided and why, then keep moving - blocking on questions is the exception, reserved for choices that are expensive to reverse.

### Surface Assumptions That Matter

If a wrong guess would materially change the work, name that assumption in one sentence and say which reading you took. Do not enumerate assumptions for their own sake - a list format invites inventing filler to complete it, and manufactured assumptions are worse than none.

### Name Conflicts, Then Proceed

On inconsistent or conflicting requirements ("X in file A but Y in file B"), name the conflict, state which interpretation you chose and why, and continue. Stop and ask only when both readings are plausible AND choosing wrong is expensive to undo. Silently picking one is the failure; halting on every ambiguity is the overcorrection.

### Push Back When Warranted

Not a yes-machine. When an approach has clear problems: point out the issue directly, explain the concrete downside, propose an alternative, then accept the decision if overridden. Sycophancy ("Of course!") followed by implementing a bad idea helps no one.

### Enforce Simplicity

Resist overcomplicating: abstractions must earn their complexity, and the boring, obvious solution usually wins. This is about structure, not line count - routing through a canonical shared utility is correct even when inlining would be shorter.

### Maintain Scope Discipline

Deliver what was asked at the scope asked. A bug or cleanup opportunity you trip over is worth one line in your summary, not a detour: do not remove comments you don't understand, refactor adjacent systems as side effects, or widen a task because something nearby looks fixable. If the discovery genuinely blocks the task, say so and fix only the blocking part.

### Dead Code Hygiene

After refactoring, identify now-unreachable code and list it in your summary. Delete it in the same change when it is unambiguously dead (only your refactor referenced it); list it and leave it when there is any doubt. Don't leave corpses silently, and don't silently delete code you merely believe is unused.

### Validate Before Push

Before pushing any branch, re-run the relevant formatting, lint, type-check, and test commands for the changes you made. Fix any issues those commands surface, include the fixes in the branch, and only then push or update the PR.

Local validation runs on a single OS and cannot catch platform-specific breakage (path separators, home directories, drive letters). A branch is not mergeable until both CI matrix legs, `test (ubuntu-latest)` and `test (windows-latest)`, are green; never merge on a local single-OS pass alone.

### Worktrees Go in the Worktree Directory

When you need a second checkout of this repository (a scratch branch, a merge trial, a build check against another branch), create it with `maestro-cli create-worktree --agent <your-agent-id> --branch <name> --base-branch <ref>`, never with a bare `git worktree add`. The desktop puts it in the agent's configured Worktree Directory (`~/Projects/Maestro-WorkTrees` for the main Maestro agent) and registers it as a worktree agent in the Left Bar, which is what keeps it visible to the user. Worktrees created by hand under `.worktrees/`, `~/Projects/worktrees/`, or a temp folder are invisible to the app; on 2026-09-11 a finished feature sat uncommitted in one for a day because nothing showed it existed. Remove a scratch worktree when you are done with it (`rm node_modules` first if you symlinked one in, then `git worktree remove`), and never leave work uncommitted in one.

---

## Standardized Vernacular

Use these terms consistently in code, comments, and documentation:

### Terminology: Agent vs Session

In Maestro, the terms "agent" and "session" have distinct meanings:

- **Agent** - An entity in the Left Bar backed by a provider (Claude Code, Codex, etc.). This is what users see, create, and interact with. Each agent has its own workspace, tabs, and configuration.
- **Session** (or **provider session**) - An individual conversation context within a provider (e.g., Claude's `session_id`). Each AI tab within an agent can have its own provider session. In code, the `Session` interface represents an agent (historical naming).

Use "agent" in user-facing language. Reserve "session" for provider-level conversation contexts or when documenting the code interface.

### UI Components

- **Left Bar** - Left sidebar with agent list and groups (`SessionList.tsx`)
- **Right Bar** - Right sidebar with Files, History, Auto Run tabs (`RightPanel.tsx`)
- **Main Window** - Center workspace (`MainPanel.tsx`)
  - **AI Terminal** - Main window in AI mode (interacting with AI agents)
  - **Command Terminal** - Main window in terminal/shell mode
  - **System Log Viewer** - Special view for system logs (`LogViewer.tsx`)

### Automation

- **Cue** - Event-driven automation system (Maestro Cue), gated as an Encore Feature. Watches for file changes, time intervals, agent completions, GitHub PRs/issues, and pending markdown tasks to trigger automated prompts. Configured via `.maestro/cue.yaml` per project.
- **Cue Modal** - Dashboard for managing Cue subscriptions and viewing activity (`CueModal/CueModal.tsx`)

### Agent States (color-coded)

- **Green** - Ready/idle
- **Yellow** - Agent thinking/busy
- **Red** - No connection/error
- **Pulsing Orange** - Connecting

---

## Code Style

This codebase uses **tabs for indentation**, not spaces. Always match existing file indentation when editing.

### Writing Style: No Em-Dashes or En-Dashes

**NEVER use U+2014 (em dash) or U+2013 (en dash) anywhere.** This applies to everything you write: user docs (`docs/`), in-app documentation, system prompts (`src/prompts/`), UI copy, code comments, commit messages, PR descriptions, and your own responses. Em dashes are a tell-tale sign of bot-authored text; humans almost never type them. Use one of these instead, whichever fits the sentence:

- A spaced hyphen (`-`) for an aside or appositive.
- A comma, colon, or parentheses to set off a clause.
- Two separate sentences when the clauses stand on their own.
- A plain hyphen (`-`) for numeric ranges (e.g. `10-20`; do not use U+2013 for ranges).

This is non-negotiable. If you catch an em-dash or en-dash in anything you produce or edit, replace it.

---

## Do Not Edit: `docs/releases.md`

`docs/releases.md` is generated/updated automatically during release pressing. **Never modify it manually** - even when shipping user-facing changes that would seem to warrant a release note entry. The release tooling handles it.

---

## Project Overview

Maestro is an Electron desktop app for managing multiple AI coding assistants simultaneously with a keyboard-first interface.

### Supported Agents

| ID              | Name            | Status     |
| --------------- | --------------- | ---------- |
| `antigravity`   | Antigravity CLI | **Beta**   |
| `claude-code`   | Claude Code     | **Active** |
| `codex`         | Codex           | **Active** |
| `copilot-cli`   | Copilot-CLI     | **Beta**   |
| `cursor-cli`    | Cursor CLI      | **Beta**   |
| `factory-droid` | Factory Droid   | **Beta**   |
| `grok`          | Grok CLI        | **Beta**   |
| `hermes`        | Hermes          | **Beta**   |
| `omp`           | Oh My Pi        | **Beta**   |
| `opencode`      | OpenCode        | **Beta**   |
| `pi`            | Pi              | **Beta**   |
| `qwen3-coder`   | Qwen3 Coder     | **Beta**   |
| `terminal`      | Terminal        | Internal   |

Rows are in picker order - alphabetical by display name, which is what
`PICKABLE_AGENT_IDS` sorts `AGENT_PICKER_META` into in
`src/shared/agentMetadata.ts`, the one registry all three provider pickers read.
The record's own key order does not matter; a new provider can be added anywhere
in it. Which provider a picker DEFAULTS to is a separate list,
`AGENT_AUTOSELECT_ORDER`, so the default is a real preference rather than
whatever happens to sort first. `terminal` is internal and never offered.

See [[CLAUDE-AGENTS.md]] for capabilities and integration details.

---

## Quick Commands

```bash
npm run dev           # Development with hot reload (isolated data, can run alongside production)
npm run dev:prod-data # Development using production data (close production app first)
npm run dev:web-desktop # Browser build of the renderer (WebSocket IPC bridge, port 5176)
npm run build         # Full production build
npm run clean         # Clean build artifacts
npm run lint          # TypeScript type checking (all configs)
npm run lint:eslint   # ESLint code quality checks
npm run package       # Package for all platforms
npm run test          # Run test suite
npm run test:watch    # Run tests in watch mode
```

---

## Architecture at a Glance

```
src/
├── main/                    # Electron main process (Node.js)
│   ├── index.ts            # Entry point, IPC handlers
│   ├── preload/            # Secure IPC bridge (one module per namespace)
│   ├── process-manager.ts  # Process spawning (PTY + child_process)
│   ├── agent-*.ts          # Agent detection, capabilities, session storage
│   ├── cue/               # Maestro Cue event-driven automation engine
│   ├── parsers/            # Per-agent output parsers + error patterns
│   ├── storage/            # Per-agent session storage implementations
│   ├── ipc/handlers/       # IPC handler modules (stats, git, playbooks, cue, etc.)
│   └── utils/              # Utilities (execFile, ssh-spawn-wrapper, etc.)
│
├── renderer/               # React frontend (desktop)
│   ├── App.tsx            # Main coordinator
│   ├── components/        # UI components
│   ├── hooks/             # Custom React hooks
│   ├── services/          # IPC wrappers (git.ts, process.ts)
│   ├── constants/         # Themes, shortcuts, priorities
│   └── contexts/          # Context providers (LayerStack, etc.)
│
├── web-desktop/            # Browser build of the renderer (WebSocket IPC bridge)
│
├── web/                    # PWA assets (public/) + service worker; rest is legacy dead code
│
├── cli/                    # CLI tooling for batch automation
│   ├── commands/          # CLI command implementations
│   └── services/          # Playbook and batch processing
│
├── prompts/                # System prompts (editable .md files)
│
├── shared/                 # Shared types and utilities
│
└── docs/                   # Mintlify documentation (docs.runmaestro.ai)
```

---

## Key Files for Common Tasks

| Task                                | Primary Files                                                                                                                                                                                                                                                    |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Add IPC handler                     | `src/main/index.ts`, `src/main/preload/` (one module per namespace)                                                                                                                                                                                              |
| Add UI component                    | `src/renderer/components/`                                                                                                                                                                                                                                       |
| Add browser/mobile touch affordance | `src/renderer/components/`, `src/renderer/utils/touch.ts`, `src/renderer/hooks/utils/` (gate on `isCoarsePointer()`; PWA assets in `src/web/public/`)                                                                                                            |
| Add keyboard shortcut               | `src/renderer/constants/shortcuts.ts`, `App.tsx`                                                                                                                                                                                                                 |
| Add theme                           | `src/renderer/constants/themes.ts`                                                                                                                                                                                                                               |
| Add modal                           | Component + `src/renderer/constants/modalPriorities.ts`                                                                                                                                                                                                          |
| Add tab overlay menu                | See Tab Hover Overlay Menu pattern in [[CLAUDE-PATTERNS.md]]                                                                                                                                                                                                     |
| Add setting                         | `src/shared/settingsMetadata.ts` (metadata), `src/renderer/stores/settingsStore.ts`, `src/main/stores/defaults.ts`, AND `src/renderer/components/Settings/searchableSettings.ts` + `data-setting-id` wrapper on rendered control (see [[CLAUDE-PATTERNS.md]] §3) |
| Add template variable               | `src/shared/templateVariables.ts`, `src/renderer/utils/templateVariables.ts`                                                                                                                                                                                     |
| Modify system prompts               | `src/prompts/*.md` (wizard, Auto Run, etc.) or edit via **Maestro Prompts** tab in Settings                                                                                                                                                                      |
| Customize prompts                   | Use **Maestro Prompts** tab in Settings, or edit `userData/core-prompts-customizations.json`                                                                                                                                                                     |
| Add new prompt                      | `src/prompts/*.md`, `src/shared/promptDefinitions.ts` (add to `CORE_PROMPTS` array and `PROMPT_IDS`)                                                                                                                                                             |
| Add Spec-Kit command                | `src/prompts/speckit/`, `src/main/speckit-manager.ts`                                                                                                                                                                                                            |
| Add OpenSpec command                | `src/prompts/openspec/`, `src/main/openspec-manager.ts`                                                                                                                                                                                                          |
| Add CLI command                     | `src/cli/commands/`, `src/cli/index.ts`                                                                                                                                                                                                                          |
| Add new agent                       | `src/shared/agentIds.ts`, `src/main/agents/definitions.ts`, `src/main/agents/capabilities.ts`, `src/shared/agentMetadata.ts` - see [PROVIDER-SUPPORT.md](PROVIDER-SUPPORT.md)                                                                                    |
| Add agent output parser             | `src/main/parsers/`, `src/main/parsers/index.ts`                                                                                                                                                                                                                 |
| Add parquet viewer feature          | `src/renderer/components/ParquetViewer/` (UI), `src/main/parquet/` (query engine), `src/shared/parquet/` (filter language + wire types)                                                                                                                          |
| Add agent session storage           | `src/main/storage/` (extend `BaseSessionStorage`), `src/main/storage/index.ts`                                                                                                                                                                                   |
| Add agent error patterns            | `src/shared/agentErrorPatterns.ts`                                                                                                                                                                                                                               |
| Add agent context window            | `src/shared/agentConstants.ts` (`DEFAULT_CONTEXT_WINDOWS`, `FALLBACK_CONTEXT_WINDOW`)                                                                                                                                                                            |
| Add playbook feature                | `src/cli/services/playbooks.ts`                                                                                                                                                                                                                                  |
| Add marketplace playbook            | `src/main/ipc/handlers/marketplace.ts` (import from GitHub)                                                                                                                                                                                                      |
| Playbook import/export              | `src/main/ipc/handlers/playbooks.ts` (ZIP handling with assets)                                                                                                                                                                                                  |
| Modify wizard flow                  | `src/renderer/components/Wizard/` (see [[CLAUDE-WIZARD.md]])                                                                                                                                                                                                     |
| Add tour step                       | `src/renderer/components/Wizard/tour/tourSteps.tsx`                                                                                                                                                                                                              |
| Modify file linking                 | `src/renderer/utils/remarkFileLinks.ts` (remark plugin for `[[wiki]]` and path links)                                                                                                                                                                            |
| Add documentation page              | `docs/*.md`, `docs/docs.json` (navigation)                                                                                                                                                                                                                       |
| Add documentation screenshot        | `docs/screenshots/` (PNG, kebab-case naming)                                                                                                                                                                                                                     |
| MCP server integration              | See [MCP Server docs](https://docs.runmaestro.ai/mcp-server)                                                                                                                                                                                                     |
| Add stats/analytics feature         | `src/main/stats/stats-db.ts`, `src/main/ipc/handlers/stats.ts`                                                                                                                                                                                                   |
| Add Usage Dashboard chart           | `src/renderer/components/UsageDashboard/`                                                                                                                                                                                                                        |
| Add Document Graph feature          | `src/renderer/components/DocumentGraph/`, `src/main/ipc/handlers/documentGraph.ts`                                                                                                                                                                               |
| Add colorblind palette              | `src/renderer/constants/colorblindPalettes.ts`                                                                                                                                                                                                                   |
| Add performance metrics             | `src/shared/performance-metrics.ts`                                                                                                                                                                                                                              |
| Capture/analyze perf trace          | `src/main/profiling/` (Chromium contentTracing capture), `scripts/analyze-perf-trace.mjs` (offline analysis), `CLAUDE-PERFORMANCE.md` -> Field Performance Traces                                                                                                |
| Add power management                | `src/main/power-manager.ts`, `src/main/ipc/handlers/system.ts`                                                                                                                                                                                                   |
| Spawn agent with SSH support        | `src/main/utils/ssh-spawn-wrapper.ts` (required for SSH remote execution)                                                                                                                                                                                        |
| Modify file preview tabs            | `TabBar.tsx`, `FilePreview.tsx`, `MainPanel.tsx` (see ARCHITECTURE.md → File Preview Tab System)                                                                                                                                                                 |
| Add Director's Notes feature        | `src/renderer/components/DirectorNotes/`, `src/shared/directorNotesNarrative.ts`, `src/shared/directorNotesGrouping.ts`, `src/main/ipc/handlers/director-notes.ts`                                                                                               |
| Add Encore Feature                  | `src/renderer/types/index.ts` (flag), `src/renderer/stores/settingsStore.ts` (default), `src/shared/plugins/first-party.ts` (tile + per-feature settings body), gate in `App.tsx` + keyboard handler                                                             |
| Modify history components           | `src/renderer/components/History/`                                                                                                                                                                                                                               |
| Modify history activity graph       | `src/renderer/components/History/ActivityGraph.tsx`, `src/main/utils/history-bucket-cache.ts` (disk-cached aggregates), `src/main/utils/history-bucket-builder.ts`                                                                                               |
| Modify Auto Run Thought Stream      | `src/renderer/stores/thoughtStreamStore.ts` (one timeline of thoughts + tool calls, `buildActivityFeed`), `ThoughtStreamPanel.tsx`, `hooks/agent/internal/useThoughtStream{Capture,Tool}Listener.ts`, `src/renderer/utils/toolActivityLabel.ts`                  |
| Add Cue event type                  | `src/main/cue/cue-types.ts`, `src/main/cue/cue-engine.ts`                                                                                                                                                                                                        |
| Add Cue template variable           | `src/shared/templateVariables.ts`, `src/main/cue/cue-executor.ts`                                                                                                                                                                                                |
| Modify Cue modal                    | `src/renderer/components/CueModal/`                                                                                                                                                                                                                              |
| Configure Cue engine                | `src/main/cue/cue-engine.ts`, `src/main/ipc/handlers/cue.ts`                                                                                                                                                                                                     |
| Add terminal feature                | `src/renderer/components/XTerminal.tsx`, `src/renderer/components/TerminalView.tsx`                                                                                                                                                                              |
| Modify terminal tabs                | `src/renderer/utils/terminalTabHelpers.ts`, `src/renderer/stores/tabStore.ts`                                                                                                                                                                                    |

---

## Critical Implementation Guidelines

### Every Surface Needs Three Ways In and Two Ways Out

A modal, dashboard, or panel the user opens is not done until it has a **hotkey**
(`DEFAULT_SHORTCUTS` + `useMainKeyboardHandler`), a **command palette** entry
(`QuickActionsModal/commands/`), and a **menu** item (`HamburgerMenuContent.tsx`,
skipped only for an in-the-moment show/hide toggle), plus an entry in
`UI_SURFACES` (`src/shared/uiSurfaces.ts`). It also needs **Escape and a visible
close control** (`<EscCloseButton>` or the `Modal` X), and a `resizeKey` so its
size is remembered. If its content owns live state, closing must park it
(`Modal`'s `hidden` prop), not unmount it. Full rules:
[UI-PATTERNS.md → Every Surface Needs Three Ways In](docs/agent-guides/UI-PATTERNS.md#every-surface-needs-three-ways-in-and-two-ways-out).

### Click-Driven Modals: Disable Text Selection

If a modal's primary purpose is _clicking_ (buttons, tabs, list rows, cards, graph nodes, filter chips, toggles), put `select-none` on its root container. Native browser drag-to-select highlighting fires accidentally during normal interactions and looks broken. Inputs and textareas keep working - Chromium preserves form-control selection regardless of ancestor `user-select: none`. For any nested subtree that's content-driven (detail views, code editors, log entry bodies, file paths, AI output, error messages), apply `select-text` on its root to opt back in. Skip the rule entirely on modals whose main purpose is reading or editing text (`CueYamlEditor`, `CueHelpModal`, wizard chat shell, System Log Viewer, confirmation dialogs). Decide click- vs content-driven when adding a new modal - retrofitting later means hunting down every nested view that needs `select-text`. Full rationale in [UI-PATTERNS.md → Text Selection in Modals](docs/agent-guides/UI-PATTERNS.md#text-selection-in-modals).

### Error Handling & Sentry

Maestro uses Sentry for error tracking. Field data from production crashes is invaluable for improving code quality.

**DO let exceptions bubble up:**

```typescript
// WRONG - silently swallowing errors hides bugs from Sentry
try {
	await riskyOperation();
} catch (e) {
	console.error(e); // Lost to the void
}

// CORRECT - let unhandled exceptions reach Sentry
await riskyOperation(); // Crashes are reported automatically
```

**DO handle expected/recoverable errors explicitly:**

```typescript
// CORRECT - known failure modes should be handled gracefully
try {
	await fetchUserData();
} catch (e) {
	if (e.code === 'NETWORK_ERROR') {
		showOfflineMessage(); // Expected, recoverable
	} else {
		throw e; // Unexpected - let Sentry capture it
	}
}
```

**DO use Sentry utilities for explicit reporting:**

```typescript
import { captureException, captureMessage } from '../utils/sentry';

// Report exceptions with context
await captureException(error, { userId, operation: 'sync' });

// Report notable events that aren't crashes
await captureMessage('Unusual state detected', 'warning', { state });
```

**Key files:** `src/main/utils/sentry.ts`, `src/renderer/components/ErrorBoundary.tsx`

---

### SSH Remote Execution Awareness

**IMPORTANT:** When implementing any feature that spawns agent processes (e.g., context grooming, group chat, batch operations), you MUST support SSH remote execution.

Agents can be configured to run on remote hosts via SSH. Without proper SSH wrapping, agents will always execute locally, breaking the user's expected behavior.

**Required pattern:**

1. Check if the session has `sshRemoteConfig` with `enabled: true`
2. Use `wrapSpawnWithSsh()` from `src/main/utils/ssh-spawn-wrapper.ts` to wrap the spawn config
3. Pass the SSH store (available via `createSshRemoteStoreAdapter(settingsStore)`)

```typescript
import { wrapSpawnWithSsh } from '../utils/ssh-spawn-wrapper';
import { createSshRemoteStoreAdapter } from '../utils/ssh-remote-resolver';

// Before spawning, wrap the config with SSH if needed
if (sshStore && session.sshRemoteConfig?.enabled) {
	const sshWrapped = await wrapSpawnWithSsh(spawnConfig, session.sshRemoteConfig, sshStore);
	// Use sshWrapped.command, sshWrapped.args, sshWrapped.cwd, etc.
}
```

**Also ensure:**

- The correct agent type is used (don't hardcode `claude-code`)
- Custom agent configuration (customPath, customArgs, customEnvVars) is passed through
- Agent's `binaryName` is used for remote execution (not local paths)
- When the user enabled SSH but the configured remote can't be resolved, **fail
  loudly** instead of silently running locally - the user explicitly opted into
  SSH and their prompt shouldn't leak to the local machine. `wrapSpawnWithSsh()`
  does NOT throw in that case: it hands back the unmodified local config with
  `sshRemoteUsed: null`, which still carries the REMOTE's `cwd`, so taking it
  runs the agent on the user's machine against a path that is either missing or
  (worse) the wrong files. Every caller must check `sshRemoteUsed` and throw
  `sshUnresolvedRemoteMessage(sshConfig)` from `ssh-spawn-wrapper.ts`. See
  `groomContext()` and `spawnGroupChatAgent()` for the pattern, and
  `sshUnresolvedFailure()` in `src/cli/services/agent-spawner.ts` for the CLI's
  version of this.

**CLI parity:** The CLI (`src/cli/services/agent-spawner.ts`) spawns agent
processes for batch/playbook automation and honors the same SSH wrapping and
agent-config overrides as the desktop app. When adding new CLI spawn sites,
thread `sessionSshRemoteConfig`, `customArgs`, `customEnvVars`, `customModel`,
`customEffort` through to `spawnAgent(...)`. The CLI loads `ssh-spawn-wrapper`
via dynamic `import()` so the SSH chain stays out of the local hot path.

See [[CLAUDE-PATTERNS.md]] for detailed SSH patterns.

---

## Debugging

### Root Cause Verification (Before Implementing Fixes)

Initial hypotheses are often wrong. Before implementing any fix:

1. **IPC issues:** Verify handler is registered in `src/main/index.ts` before modifying caller code
2. **UI rendering bugs:** Check CSS properties (overflow, z-index, position) on element AND parent containers before changing component logic
3. **State not updating:** Trace the data flow from source to consumer; check if the setter is being called vs if re-render is suppressed
4. **Feature not working:** Verify the code path is actually being executed (add temporary `console.log`, check output, then remove)

**Historical patterns that wasted time:**

- Tab naming bug: Modal coordination was "fixed" when the actual issue was an unregistered IPC handler
- Tooltip clipping: Attempted `overflow: visible` on element when parent container had `overflow: hidden`
- Session validation: Fixed renderer calls when handler wasn't wired in main process

### CDP / Browser-Automation Scripts Are Ephemeral

When driving the running app over Chrome DevTools Protocol (e.g. one-off `scripts/cdp-*.mjs` harnesses for reproducing a bug, clicking through a flow, or capturing screenshots), treat those scripts as **throwaway**. They are debugging scaffolding, not shipped code:

- Write them under `scripts/` if you like, but **delete them when the investigation is done** - do not leave them in the working tree and do not commit them.
- If one gets committed by accident, remove it (a forward `git rm` commit is fine; avoid history surgery on `rc` unless asked).
- Heads-up on this dev setup: the dev server often runs with `DISABLE_HMR=1`, so live edits will NOT hot-reload. A full page reload only picks up new code if the vite process was started **after** the edit hit disk. Verify the served module actually contains your change (`curl localhost:17173/<module>` and grep) before trusting any CDP screenshot.

### Focus Not Working

1. Add `tabIndex={0}` or `tabIndex={-1}`
2. Add `outline-none` class
3. Use `ref={(el) => el?.focus()}` for auto-focus

### Settings Not Persisting

1. Check wrapper function calls `window.maestro.settings.set()`
2. Check loading code in `useSettings.ts` useEffect

### Modal Escape Not Working

1. Register with layer stack (don't handle Escape locally)
2. Check priority is set correctly

---

## MCP Server

Maestro provides a hosted MCP (Model Context Protocol) server for AI applications to search the documentation.

**Server URL:** `https://docs.runmaestro.ai/mcp`

**Available Tools:**

- `SearchMaestro` - Search the Maestro knowledge base for documentation, code examples, API references, and guides

**Connect from Claude Desktop/Code:**

```json
{
	"mcpServers": {
		"maestro": {
			"url": "https://docs.runmaestro.ai/mcp"
		}
	}
}
```

See [MCP Server documentation](https://docs.runmaestro.ai/mcp-server) for full details.
